# Agent handoff (Claude ↔ Codex) — cleaned up 2026-08-22

Consolidated from `raw/handoff_codex.md` (renamed) + `raw/handoff_claude.md`
(merged, then retired). Full pre-cleanup history is preserved verbatim in
`raw/archive/handoff_agent_pre-cleanup_2026-08-22.md`. Every task below is
2-3 sentences; anything with real detail (SHAs, run IDs, incident timelines,
backtest numbers) points to a file under `raw/explain/`.

**Prefix format (mandatory):** `[Business][Priority][Created at] Title:`
- Business: `observability`, `trading`, `affiliate`, `english` — this repo
  only ever uses the first two; the other two exist for other business
  lines sharing this convention.
- Priority: `risk` (security/critical, unresolved) > `high` > `medium` >
  `low`; `evidence` for anything already resolved/closed and kept only as a
  record (all of `## Verify` and `## Done`), not an open action item.
- **[trading][evidence][round 63-167] Target 2 (Make Decision rate) — floor `interval_quality` in `reweight_from_alpha_performance`, IMPLEMENTED + DEPLOYED + VERIFIED:** root cause found Round 65 (asymmetric strategy subscription accidentally floors some routes' interval_weights via untested-strategy benefit-of-doubt), a bad fix (subscribe another zombie strategy) proposed then retracted Round 67, the real fix designed+quantified Round 165-166 (Python reproduction against real Redis-read production data, exact match confirmed), implemented Round 167 as `MultiTimeframePortfolioPolicy::INTERVAL_QUALITY_FLOOR=0.05` scoped to interval-quality only (never strategy-quality, preserving "confirmed losers get zero" design intent). Deployed `7fe0e13`; all 4 routes' previously-zeroed `5m` interval_weights now match the pre-deploy simulation to 6+ significant figures exactly (XAU/binance 0.0479, BTC/binance 0.0109, BTC/exness 0.0108, XAU/exness 0.0161). All 6 containers healthy on exact SHA, `evaluation_count` advancing normally all 4 routes. Follow-up: check production 1-2 days later for actual `trades_per_week` movement. Details → `raw/researcher/round165-target2-interval-weight-floor-proposal.md`.

- **[trading][evidence][round 149] Vortex Indicator + Realized Volatility Regime Filter — CLOSED, both falsified on 5-year honest BTC/binance backtest:** `vortex_indicator_14` PF 0.34-0.39 across all 3 splits (train/validation/holdout), no false-positive shape. RV-regime-filter variants on `candle_momentum`/`rsi_mean_reversion` all stay PF<1 across all 3 splits too (best variant holdout PF 0.553). Neither promoted; code stays in working tree unpromoted per convention. Also a second independent confirmation the `MaxConnectionAge` gate fix (Round 142) works across repeated cycles. Details → `raw/researcher/round149-vortex-and-rv-regime-filter-closed-5y-honest.md`.

- Created at: real date when known (`YYYY-MM-DD`); `round N` when only a
  `/loop` round number was recorded and no calendar date exists; `n/a` when
  neither is known — never a guessed date.

## Processing

- **[trading][medium][2026-08-27] Kline stream flow-control stall — defensive server mitigation đã commit local:** `finance-mw` commit `a9349fd` bounds mỗi `stream.Send` lịch sử ở 5 phút theo tiến độ, trả `DeadlineExceeded` và nhả gate khi reader không còn drain HTTP/2; regression test tái hiện `Send` bị block và chứng minh abort bounded. Full Go test/vet/build và `git diff --check` xanh; chưa push/CI/Coolify/production, client replay reader root cause vẫn cần xác minh sau deployment.

- **[observability][high][2026-08-27] Chuyển ownership Docker infrastructure/observability sang finance-workspace — seed local xong, consumer cutover còn Processing:** đã seed `docker/infrastructure` (20 file) và `docker/observability` (41 file, gồm POC Kafka/ClickHouse/S3), loại `.env` credential và `.env.poc`; workspace commits `bc37cfe` + `846223c` + `9a1e46b` (chuẩn hóa trailing whitespace, không đổi semantics). Finance MW commit `ad43acd` đã loại toàn bộ consumer web khỏi pipeline; commit local `cc4e65a` chuyển Makefile cùng ba script Grafana sang resolver `FINANCE_WORKSPACE_ROOT`, commit `0fadc9e` chuyển validator tracing, Kibana rotation script/test, OpenTelemetry contract test, commit `ab4864c` chuyển các asset Elasticsearch trong `coolify-resources.sh`, commit `c9078f0` chuyển bốn contract test Compose/Filebeat/Elasticsearch/Kline-maintenance, commit `5e05a73` chuyển contract healthcheck, commit `bde66e7` chuyển bốn Python contract test memory/process/Prometheus/retired-metrics, commit `474b337` bổ sung helper vào path filters monitor/deployment để các thay đổi resolver không bị skip lane, và commit `943d2de` làm `test_docker_layout.sh` validate canonical workspace (loại false-fail do thư mục `docker/monitor` root-owned chỉ còn trong compatibility checkout) (fallback compatibility copy cho CI checkout đơn repo). Test 80/80 + tracing 10/10 + Kibana contract + infrastructure healthcheck + Compose 43 services + Filebeat + Elasticsearch retention + Kline JSON transport + validator Docker + monitoring contracts 19/19 + Docker layout + workflow YAML parse + `make -n` đều xanh. Các workflow vẫn chưa checkout workspace canonical vì cần remote SHA được owner xác nhận; Coolify scripts/tests khác còn đọc path Docker trực tiếp nên compatibility copy chưa thể xoá an toàn. Chưa push/CI/Coolify.

- **[trading][low][2026-08-23] SMA(10) + Fibonacci(100) Round106 — near-miss đã CLOSED, implementation local đã sạch:** isolated FLA commit `6e299bf32d6db11a917aabda9acb22e0da8ca1b4` trên Round105 `4d45eb0` đăng ký đúng một composition; docs correction descendant `70210debafb4818614589047df229c5d67a0a88e` lưu comparative strict-vs-draft Round103 và không giả Round104 đã rerun strict-inner. Published sweep dùng Fibonacci first-tied-extrema trong khi parent khóa latest-tie đúng mô tả nên permanent caveat giữ rõ không exact-replay; Docker CPU3 fmt + full `finance-research` 107/107 + build xanh, root review logic/diff/attribution đạt. Chưa push/deploy để giữ exact FLA pin cho XAU; sẽ nằm trong combined linear chain sau marker backfill. Details → `raw/researcher/round106-sma10-fibonacci-near-miss-reverses-on-18mo-window.md`.

- **[trading][low][n/a] Fibonacci Golden Zone retracement Round105 — research CLOSED, implementation local đã sạch:** isolated FLA commit `4d45eb05209224367d39caa4c0085c0ef3977353` trên Round104 `a48b23d` chỉ thêm `FibonacciGoldenZoneStrategy`, period `20/50/100`, permanent caveat và regressions prior-window/no-lookahead/current-close/latest-tied-extrema/instrument+timeframe/registry; Docker CPU3 fmt + full `finance-research` 106/106 + build xanh, chưa push/deploy. Root review logic/diff đạt; correction audit giữ rõ bảng thực tế `35/36` cells PF<1, ô XAU/Binance period20 validation `1.068` nhưng train `0.786`/holdout `0.378`, không variant nào pass đủ 3 split nên không promotion. Draft chọn extrema occurrence đầu khi tie trái “most recent swing leg”; commit khóa occurrence gần nhất, vì vậy số PF cũ không exact-reproduce tie semantics mới và report cũng thiếu immutable artifact/image digest. Details → `raw/researcher/round105-fibonacci-golden-zone-closed-different-failure-mode.md`.

- **[trading][low][2026-08-23] SMA(10) trend filter on Round103 `engulfing_pattern` — CLOSED về research nhưng đang lưu implementation:** isolated FLA commit `a48b23df8cfeea006558cf00fe1181fa05779e0a` trên Round103 `06f8bed` chỉ thêm `SmaTrendFilterStrategy`, đúng một candidate, replay identity và regression current-closed/SMA agreement/instrument+timeframe isolation/registry; Docker CPU3 fmt + full `finance-research` 102/102 + build xanh, chưa push/deploy. Root review implementation/order đạt và comment đã ghi rõ methodology mismatch: Round103/104 backtest draft dùng `previous_bullish == current_bullish`, nên prior doji có thể được coi như nến non-bullish để trigger Long; strict Round103 parent chưa rerun, vì vậy số PF cũ không được claim là exact reproduction trước comparative. Details → `raw/researcher/round104-sma10-trend-filter-engulfing-still-closed.md`.

- **[trading][low][2026-08-23] Two-candle engulfing pattern Round103 — CLOSED về research, implementation local chờ giải quyết exact semantics:** isolated FLA commit `06f8bed840b4f93540ff04f6c744f639feaf3e90` trên Round99 `5aeaeb6` chỉ thêm `EngulfingPatternStrategy`, một research candidate và ba regression previous-closed/no-lookahead/timeframe/doji; Docker CPU3 fmt + full `finance-research` 98/98 + build xanh, chưa push/deploy và không chạm checkout Claude dirty. Root review xác nhận report mô tả strict prior red/green nhưng draft thực sự dùng boolean equality khiến prior doji có thể trigger bullish; commit local đã sửa strict semantics nên comparative rerun đang chờ MW deploy ổn định, không được tuyên bố tái tạo exact số cũ trước kết quả. Details → `raw/researcher/round103-engulfing-pattern-closed-worst-cost-drag-yet-observed.md`.
  - **Comparative strict-vs-draft 10:34 UTC+7:** same current BTC/Binance `5m/1825d` snapshot có `525,599` candle, continuity 8 interval zero gap và 51 strategy khác byte-identical. Draft→strict real-cost train `34055/.3988466→33919/.3994810`, validation `11028/.3594342→10994/.3601916`, holdout `11199/.3468520→11135/.3483085`; zero-cost PF `.9741981/.9851230/.9939087→.9734209/.9856634/.9951064`. CLOSED/cost-limited không đổi; đây là current equal-snapshot comparison, không giả là immutable original vì report cũ thiếu candle fingerprint/exact command. Đã giao permanent comment correction thành commit docs riêng sau Round106, không rewrite stacked history.
  - **Quant xác nhận độc lập 10:xx UTC+7 (Round 105):** đã áp đúng fix strict-semantics của Codex vào working copy, rebuild, rerun BTC/binance 5 năm để đo tác động thật: holdout PF 0.347→0.348 (Δ0.001), trades 11,197→11,134 (Δ0.6%) — không đáng kể, khớp kỳ vọng vì doji thật hiếm trên dữ liệu 5m thực. Kết luận CLOSED của Round103/104 giữ nguyên, đã ghi addendum vào cả 2 file round. Cảm ơn Codex đã bắt lỗi qua root review — đây đúng là bug thật trong code của Quant, không phải false alarm.

- **[observability][high][2026-08-23] Finance web bị gián đoạn mỗi lần Coolify deploy Docker Compose:** production probe trong full delivery `32610903170` bắt `/` đổi `200→502/503` từ 09:01:51 tới 09:02:45 UTC+7 (~54 giây), sau đó tự hồi `200`; container mới `finance-web-1` healthy và JS/CSS/health/version đều phục hồi. Root cause đã chứng minh: web là Docker Compose app một replica nên Coolify recreate backend duy nhất; fix verifier `848d236` chỉ tránh verify xuyên rollout, không tạo zero-downtime. Tài liệu Coolify v4 hiện tại xác nhận rolling update giữ container cũ tới khi container mới healthy nhưng **không hỗ trợ Docker Compose deployment**; fix bền vững cần migration resource web sang Dockerfile build pack (healthcheck hợp lệ, expose 80, không host port/custom container name) hoặc kiến trúc load balancer/replica riêng, đây là thay đổi resource production cần review/plan trước khi mutate.
  - **Tái hiện delivery 11:03-11:05 UTC+7:** CI `32616572974` phải publish/deploy web vì OOM lane có đổi `ci-cd.yml`, dù không đổi web business code. Probe 2 giây bắt một nhịp cả `/` và `/healthz` timeout lúc 11:03:33, hồi 11:03:39 trong phase BuildKit publish (correlation host/build load, nguyên nhân riêng còn unknown); khi `Deploy web` bắt đầu, cả hai lại timeout liên tiếp 11:04:36/11:04:42 và hồi 200 lúc 11:04:48, tái chứng minh Compose single-replica rollout có user-visible outage. Giữ task Processing; không gộp build-stall hypothesis vào proven rollout root cause.
  - **Incident BuildKit 11:05-11:15 UTC+7:** cùng run `32616572974`, sau rollout probe tiếp tục bắt `/` + `/healthz` xen kẽ `502/503/timeout` dày từ 11:05:52 trong khi `Publish runtime image` chạy trên self-hosted `finance-mw-ci` cùng production host. Workflow thực tế cho BuildKit tới `4 CPU/6 GiB` trên host khoảng 8 GiB; cancel thường lúc 11:10:33 chưa được acknowledge, force-cancel API trả `202` lúc 11:12:03, job/run thành `cancelled` 11:12:04 và không có runtime/worker deploy. Lỗi HTTP 502/503 dừng sau 11:06:34; timeout thưa dần và probe liên tục `200/200` từ 11:13:10 tới ít nhất 11:14:11. Correlation thời gian + hồi sau stop rất mạnh nhưng chưa có host pressure time-series nên ghi causal là build pressure `probable`, không giả thành OOM. Regression đỏ→xanh hạ cả ba Buildx builder còn `2 CPU/3 GiB`, khóa `memory-swap=3g`, bắt source-current reconcile+verify trước build để daemon cũ không nâng ngược; commit `68235350ffc0cc7e4e916995573230144aaf4b33` đã push, CI `32617410442` đang được watcher theo dõi với probe live. Zero-downtime rollout vẫn là task kiến trúc riêng và còn Processing.
  - **Independent web-revision detector delivered 11:20-11:39 UTC+7:** run `32617410442` đã cancel trước publish khi regression audit phát hiện CI lấy deployed MW SHA để suy ra web diff, nên có thể redeploy web dù web production đã exact revision riêng. Commit `7f70eb360dc95e60a6f3ba0533f51747f8170f03` thêm detector fail-closed đọc đúng một `finance-web-source-commit` meta từ homepage và tính web diff từ SHA đó; local detector/transport/duplicate/missing/workflow/actionlint đều xanh. CI/CD `32617608885` xanh toàn bộ; web source đang `7988c3a14b73f2763bbe7dedad7e43eba6458b2d`, vì vậy `Test/Publish/Deploy web` đều skip trong khi runtime build/publish/deploy vẫn hoàn tất. Probe xuyên BuildKit `2 CPU/3 GiB` và runtime rollout không ghi nhận HTTP `502/503`; chuỗi `000E` lúc 11:34-11:36 được chứng minh là DNS client vì `github.com` cũng không resolve, còn pin SNI/IP production thì `/`, `/healthz`, `/api/v1/system/version` đều 200. Fix này tránh rollout web thừa; zero-downtime cho một rollout web thật vẫn cần migration resource/replica và task giữ Processing.
  - **Recheck độc lập 12:02 UTC+7:** domain production đúng `https://finance.thanhne.io.vn` trả `/=200` trong `0.039s`, `/healthz=200` trong `0.037s`; deployment inventory `complete=true`, MW exact `7f70eb360dc95e60a6f3ba0533f51747f8170f03`, broker exact `f7a3f18c15994397a2f2f79c3860b36f1553b45a`, 4/4 FLA exact `56437f2d93c0c47f76e936b0a237ec72baa5a6b6`, tất cả `status=ok`. Web hiện không die; task còn mở chỉ vì rollout Compose thật vẫn chưa zero-downtime.
  - **Rollout thật tái hiện lần ba 13:12-13:13 UTC+7:** MW run `32621430881` có thay Sidebar nên phải publish/deploy web. Probe 2 giây ghi last-good 13:12:35, liên tục `/` + `/healthz` `502` từ 13:12:38, chuyển `503` lúc 13:13:09-13:13:22, first-good 13:13:24 — khoảng 46 giây user-visible downtime; control `github.com` vẫn `200` xuyên gần toàn window (một mẫu network timeout riêng lúc 13:12:49), nên chuỗi HTTP 502/503 này là production thật. Coolify job vẫn xanh vì container mới cuối cùng healthy; đây tiếp tục chứng minh Compose single-replica rollout, không phải web app business crash và không được coi deploy-green là zero-downtime.

- **[observability][risk][2026-08-22] Nhiều credential trong tracked `docker/env/production.env` bị lộ qua root diff review:** khoảng 22:23 UTC+7, Codex review commit OOM bằng `git show` có context file env và output đã in các value của `NOTION_API_KEY`, `BROKER_CREDENTIAL_ENCRYPTION_KEY`, `DATABASES_PASSWORD`, `REDIS_PASSWORD` (ngoài token Telegram đã log riêng). Đã dừng mọi diff/show/grep rộng có thể chạm env, không chép value/hash/length vào handoff; cần inventory exact consumers bằng key allowlist, rotate từng credential live-first, chuyển durable owner sang Coolify/GitHub secret không track, regression scan và chứng minh old rejected/new accepted mà không dump env/argv.
  - **Guarded source lane local 22:39 UTC+7:** branch sạch có commit `29878d2b33dbe0442f413c5c8258b8b0a76129d1` thêm plan/install/restore source với exact MW/worker allowlist, SHA+confirmation, mode-0600 payload và rollback; commit kế `b344c529ac9afcd96505a7171ad5b438e8061b4d` xóa đúng 4 assignment tracked, thêm scoped Compose mapping/regression/runbook. Full bounded shell/Compose, Go test/vet và năm Go build đều xanh; chưa push/API/prod/read value. Delivery phải tách: merge/install commit 1, owner nạp `CURRENT_*` vào GitHub production qua stdin và verify Coolify rồi mới merge commit 2; Notion/broker-encryption/DB rotation cần owner maintenance, Redis còn phải hội tụ Redis owner + 4 FLA Compose/consumer nên giữ Processing.

- **[observability][risk][2026-08-22] `FACEBOOK_TELEGRAM_BOT_TOKEN` plaintext bị track và lộ qua diagnostic grep:** khoảng 22:11 UTC+7, subagent audit backlog chạy `rg` tìm `1m` trên phạm vi vô tình gồm `docker/env/production.env`; match xảy ra bên trong value và tool output đã in token. Đã dừng mọi scan rộng trên env, không chép value/hash/length vào handoff; cần rotate bot token live-first qua owner Telegram/Meta tương ứng, chuyển source sang Coolify/GitHub secret không track, thêm regression secret scan và chứng minh old bị từ chối/new accepted trước khi chuyển Verify.
  - **Local guarded lane 22:28 UTC+7:** commit local sạch `66dd86d060297ce36289965e31100ccf1a888a1f` trên base `fa52121` đã bỏ assignment tracked, chỉ inject token vào job-worker qua Coolify locked literal var do GitHub production environment secret cấp, đồng thời thêm exact-main/old-401/new-identity/deploy/doctor/rollback guards và secret-scan regression; Go test/vet/build cùng shell/Compose/Coolify contracts đều xanh. Chưa push/mutate vì GitHub production secret hiện chưa tồn tại và BotFather `/revoke` là bước owner-gated không rollback được; cần owner cấp token hiện tại qua stdin và thực hiện revoke riêng tư rồi mới deliver/dispatch.

- **[observability][risk][2026-08-22] Elasticsearch/Kibana credential và encryption-key bị lộ lại qua process-argument diagnostics:** lúc 20:49 UTC+7, Codex chạy read-only `ps ... args` để audit BuildKit bị chậm; output vô tình chứa Kibana service password cùng ba Kibana encryption-key do container truyền secret qua argv. Đã dừng ngay mọi process-argument dump rộng, không chép bất kỳ value nào vào handoff; cần live-first rotate lại service credential và lập kế hoạch xoay các encryption key có decryption-only/rollback để không làm mất saved objects/session/report, sau đó sửa source/diagnostic guard để không tái diễn. Chưa mutation; ưu tiên sau khi CI/deploy MW hiện tại ổn định vì rotation chạm observability production.
  - **Automation source 21:17-21:51 UTC+7:** guarded workflow/source đã review hai vòng và push trong descendant `ea2e1186ffa3a6749662413e65cb1dd048e5c97b` (Kibana commit `01c4c1d`): source-plan/install-source có exact raw-Compose digest, mode-0600 backup/readback, byte-exact Coolify PATCH, deploy/verify/automatic rollback; entrypoint chuyển service password + ba key qua stdin vào ephemeral keystore và xóa chúng khỏi argv; saved-object rotation có snapshot, hai pass fail-closed, decryption-only/finalize/rollback. Regression đã bắt và sửa lỗi payload base64 trước merge, bao phủ install failure + exact source rollback; Kibana/Coolify/Compose/health/monitor và 112 Python tests xanh, CI `32579854086` đang chạy. Chưa production mutation/credential read; security/reporting key vẫn cần maintenance được owner chấp thuận vì xoay security key vô hiệu toàn bộ session và reporting key có thể làm hỏng report đang chờ.
  - **CI fix-forward 21:51-21:56 UTC+7:** run `32579854086` fail ở deployment-script test vì self-hosted runner không cài module `yaml`, sau khi XAU guard và toàn bộ check trước đó đã pass; không publish/deploy. Đã bỏ dependency ngầm bằng shell byte-exact extraction của inline entrypoint, thêm contract cấm mọi test command không có hard timeout và bọc nốt ba command cũ; full local deployment-script baseline lặp lại xanh. Fix `fa52121be7efcd057b7b9fb471cbd4932b3b7fb7` đã push, retry CI `32580669994` đang được watcher theo dõi.

- **[observability][risk][2026-08-22] MT5 plaintext credential đang được track trong Compose và bị lộ qua diagnostic context:** lúc audit `MT5_MAX_BARS` khoảng 21:29 UTC+7, output subagent vô tình kèm dòng credential kế bên trong `mt5/docker/compose.yaml`; không chép value/hash/length vào handoff và đã dừng mọi dump config rộng. Cần xác định exact key/consumer bằng parsing allowlist, rotate production credential live-first, chuyển Compose sang secret/env source không track, thêm regression secret scan, commit/push/CI/deploy MT5 rồi chứng minh old credential bị từ chối; chưa mutation vì network tới GitHub/production đang mất route.
  - **Delivery 21:50-22:08 UTC+7:** official upstream + source xác nhận đây là `PASSWORD` của KasmVNC HTTP Basic (user `CUSTOM_USER`), không phải MT5/Exness broker credential. Commit remote `b83947eec4e0153fa58d3031da63f9985d6f689b` bỏ plaintext, bắt buộc `MT5_VNC_PASSWORD` do Coolify sở hữu, thêm secret-scan/Compose regression và workflow plan/apply có exact current-main guard, stable-vs-immutable image proof, targeted runtime bootstrap, Coolify backup, exact-SHA deploy, new-accepted/old-401 và automatic rollback. Full Docker `--cpus=2` local: 32 Python tests + 7 shell suites, workflow YAML, Compose config, build/runtime config, syntax/diff đều xanh; replacement đã stage kín trong GitHub production secret qua stdin, CI `32580766025` đang chạy. Chưa đọc/ghi giá trị vào log/handoff; chờ immutable image build rồi guarded plan/apply và rerun CI nếu initial auto-deploy fail-closed do live Coolify chưa có key.
  - **Plan fix-forward 22:13-22:18 UTC+7:** CI/deploy `32580766025` xanh exact `b83947e`, nhưng read-only plan `32581108669` fail-closed vì Coolify có một production row và một preview row cùng key; metadata-only DB query xác nhận cardinality `is_preview=false/true`, không đọc value. Regression đỏ→xanh đã sửa automation chỉ xét production row và vẫn fail duplicate production/rollback; Docker `--cpus=2` lặp lại 32 Python + 7 shell suites xanh. Fix `ef9257a4493b669b1b735884467e0aaa49a42f16` đã push, CI `32581265073` đang chạy; chưa rotate/mutate credential.
  - **Opaque-secret fix-forward 22:24-22:28 UTC+7:** CI/deploy `32581265073` xanh exact `ef9257a` và public version trả đúng SHA, nhưng plan `32581617536` fail-closed vì Coolify API che value production trong khi runtime credential đã capture an toàn; không mutation. Regression đỏ→xanh nay phân biệt value rỗng/toàn dấu mask là opaque, vẫn fail nếu API trả plaintext khác runtime, và plan masked không phát sinh POST/PATCH/DELETE; toàn bộ 7 shell suites xanh. Fix `5cbabf1453696c948cef9ca2967a5eb308115d42` đã push, CI `32581764260` đang chạy; replacement vẫn stage kín, chờ deploy exact SHA rồi rerun plan/apply.
  - **Permission gate 22:39-22:43 UTC+7:** CI/deploy `32581764260` xanh và public MT5 exact `5cbabf1` status `ok`; read-only plan `32582335623` xanh. Apply `32582381141` dừng trước credential update/deploy vì Coolify trả HTTP 403 khi POST biến rollback; runtime vẫn dùng credential cũ, không partial rotation. Coolify OpenAPI hiện hành xác nhận endpoint/method POST `/applications/{uuid}/envs` là đúng và 403 nghĩa token thiếu permission `write` (token hiện vẫn đủ read/deploy, `read:sensitive` không có nên value masked); replacement còn stage kín. Cần owner cấp `write` cho production `COOLIFY_TOKEN` rồi rerun plan/apply, không hạ chuẩn durable rollback.
  - **Gate recheck 23:59 UTC+7:** metadata targeted xác nhận repo secret `COOLIFY_TOKEN` vẫn có `updatedAt=2026-07-26T13:06:50Z`, chưa được owner thay token có scope `write`; `MT5_VNC_PASSWORD_NEXT` vẫn stage trong production environment. Không retry apply vô ích, không đọc value và không có production mutation.

- **[observability][risk][2026-08-21] `KAFKA_CONTROLLER_PASSWORD` exposed via a broad `docker exec ... env` dump:** value chưa từng được log lại; rotation KRaft phải fail-closed để không làm gãy quorum. Automation guarded plan/apply/auto-rollback đã được rebase lên MW `2fe05ffd3bc0d62cfabff89dd1883cfa42d4e87b` thành local descendant sạch `b084bb1f848dbc96dba7f6bfd7beeffe6affeb6e`: plan read-only; apply có confirmation/exact-SHA/concurrent-overwrite guards, sparse volume backup, Coolify-owned stop/patch/start, xác minh new-generation/quorum/topic/ACL/consumer lag/app identities/public health, tự rollback khi hậu kiểm fail; không có broad env/secret output. Review phát hiện và regression-first sửa thêm 3 test CI thiếu hard timeout; full deployment 17/17, infra/observability/Compose/Python/Grafana/tracing contracts đều xanh. Chưa push/dispatch/đọc credential/mutate production; giữ Processing tới khi app gate MW hiện tại ổn định, review diff root xong rồi chạy plan và chỉ apply sau khi exact backup/digest/quorum evidence đạt. Chi tiết → `raw/explain/kafka-controller-password-exposure.md`.
  - **Rebase + functional hardening 22:20 UTC+7:** automation đã thành một commit sạch `b2c0e546d6ee6885beef0903e5428c6bd2a9517b` trên exact base `fa52121`, giữ nguyên contracts Kibana/XAU. Regression functional chứng minh plan không mutation/backup/credential change và xuất Compose digest; digest mismatch chặn trước mutation; apply giả lập start failure tự rollback đúng old credential + recovered generation với đúng 2 PATCH/1 STOP/2 START và không lộ secret. Bash/YAML, Kafka/Kibana/XAU/Coolify contracts và diff-check xanh; chưa merge/push/dispatch/prod/read credential, chờ XAU + Kibana lane exact `fa52121` hoàn tất trước khi đổi main.
  - **Root review fix-forward 22:28 UTC+7:** final local commit `41cb4fe9932df932fbd11503a4eb02a5d4a11e0d` sửa fleet gate dùng chung maintenance-readiness helper (chỉ chấp nhận Exness XAU worker-ready=0 đúng UTC Saturday, mọi dependency vẫn bắt buộc 1) và chỉ chọn/backup Coolify production rows khi có preview duplicate. Functional plan Saturday không mutation và apply-failure rollback regression đều xanh; worktree sạch, chưa merge/push/production để giữ exact SHA cho XAU/Kibana lane đang chạy.
  - **Rebase 22:47 UTC+7:** Kafka automation đã rebase sạch lên XAU guard `a7ad98f`, final local `d80f04fe09f4ca96298941b61311c8646b55eb94`; XAU readiness/image-ID files giữ đúng target. Kafka functional/workflow/readiness, YAML, shell và diff checks đều xanh; chưa push/deploy/credential/prod, chờ XAU exact lane đóng.
  - **Rebase + test isolation 00:13 UTC+7:** final local `2933f2bb0b849709ef3607f8426d3a77211ba233` trên exact `a7494ef`, worktree sạch, preserve XAU trailing và web-verifier dependency. Review giữ đầy đủ exact source/image/Compose digest, backup 0600, stdin payload, quorum/topics/ACL/lag/app-readiness/public health và automatic restore/reconverge; regression flaky do test đọc CPU PSI host thật đã tách qua override chỉ trong `KAFKA_ROTATION_TEST_MODE`, production vẫn đọc `/proc/pressure/cpu`. Năm lượt functional song song cùng workflow/readiness/Coolify/YAML/shell/Compose/memory budgets xanh; chưa push/plan/apply/prod, chờ XAU lane đóng.

## Todo

- **[trading][evidence][2026-08-27] Cập nhật độc lập task BTC Portfolio zero-decision:** mục `[trading][high][2026-08-25]` bên dưới đã được xác nhận hồi phục qua production read-only lúc 15:02 UTC+7. Cả 6 Live Action container đang chạy image `finance-live-action_sha-4bce2851389ac6d7c6a3b6c7f270e0a4bf3f5c22`, `evidence_intervals_complete=8/8`, `replay_scopes_completed=3`, và `portfolio_decisions_total` hiện diện/tăng (Binance BTC `181`, Exness BTC `493`); chưa có CI/Coolify action mới. Claude cần chuyển entry gốc sang Verify và review độc lập, không coi đây là Done chỉ từ bằng chứng Codex.

- **[trading][high][2026-08-27] Follow-up XAU/binance freeze:** production read-only lúc 15:02 UTC+7 cho thấy route đã có `evidence=8/8`, `replay_scopes=3`, `portfolio_decisions_total=193` trên image `4bce285`; điều này chứng minh worker đang evaluate nhưng chưa chứng minh đã re-enter/ghi trade mới. Giữ nguyên kết luận Round 203 (trend-score/edge chưa đạt), không tự nâng floor hoặc unfreeze khi chưa có backtest out-of-sample và trade-log evidence.

- **[trading][evidence][2026-08-25] Swing 4h/1d sweep on XAU (never run before) — CLOSED, binance falsified by exness:** Round 17 scoped the swing family to BTC only, so this was genuinely untested for XAU and was the right next experiment (the only confirmed edge in program history is 4h/1d; Round 204 reconfirmed filter>oscillator; Round 203 showed nothing in the 5m registry qualifies for XAU/binance). Binance produced four candidates clearing 1.0 on all three splits — `engulfing_pattern` **1.44/1.58/1.73**, `heikin_ashi_momentum_3` 1.14/1.51/1.16, two `mtf_stochastic` variants — but **exness inverts every one** (`engulfing_pattern` 0.65/0.86/0.70 on 322/107/106 trades vs binance's 75/27/32). Cause: binance XAU holds only 1,543 4h candles (~257 days) against exness's 7,986 (~3.6 years); the thin short-window sample is the artifact, same signature that falsified ATR breakout (r61), Donchian (r88), Fibonacci (r106). **Explicit warning recorded: do NOT re-open 5m-closed mechanisms at 4h on binance XAU evidence alone** — `engulfing_pattern` was the lowest-PF candidate ever recorded at 5m (r103, 0.16-0.42) and 4h does not rescue it. One weak lead kept, not promoted: `mtf_stochastic_14_3_30_70_sma10_trend_filtered` is the only candidate with train>1 and holdout>1 on both brokers (1.09/1.16, 1.19/1.79), but exness validation dips to 0.70 and the binance sample is 18/7/9. **Net: still no validated strategy to add to XAU/binance**, so its frozen Portfolio stays frozen by choice — and per Round 203 its unfrozen behavior backtests to −1.54 anyway. Details → `raw/researcher/round205-xau-swing-4h1d-closed-cross-broker.md`.

- **[trading][evidence][2026-08-25] Larry Connors RSI(2) — CLOSED, falsified cross-broker on XAU:** genuinely new published mechanism (very fast RSI(2) + extreme 10/90 thresholds + long-term trend filter), structurally distinct from every RSI variant tried before (period 9/14, thresholds 20-35/65-80). Bare `rsi_2_10_90`: PF 0.111/0.064/0.043 (binance) and 0.024/0.028/0.106 (exness) with **41,981 train trades on exness** — pure over-trading on 5m noise, the **8th** oscillator mechanism to fail this exact way after Stochastic/CCI/MFI/OBV/Elder Ray/Vortex/Awesome. `sma200_trend_filtered_rsi_2_10_90`: 0.707/0.905/0.750 and 0.425/0.552/0.865 — the filter helps hugely (PF ~0.05 → ~0.7, reconfirming that the filter not the oscillator is where edge lives) but no split clears 1.0 on either broker and the two disagree on which splits are least bad. No 18-month cross-window needed (nothing near breakeven). Also the end-to-end confirmation that the Round 200/201 deadlock fix works: this is the candidate that failed 7 consecutive times earlier today, and both runs completed first-attempt with the gate free (`in_flight=0`). Details → `raw/researcher/round204-connors-rsi2-closed-cross-broker.md`.

- **[trading][high][2026-08-25] XAU/binance frozen since 2025-12-26 — mechanism fully diagnosed, proposed fix REJECTED by its own backtest, and unfreezing turns out NOT to be the urgent problem:** `dsc=69,710` (~242 days). Gate needs `|entry|>=0.10` AND `|trend|>=0.10` AND same sign; live values are `entry=+0.14348` (clears) and `trend=-0.03899` (too small AND opposite sign). Cause: `rsi_mean_reversion` holds **62.2% of strategy weight** while reporting `Hold` at both `1d` (weight 0.430) and `12h` (0.190) — 62% of all trend weight — because RSI(14) rarely reaches 30/70 on daily gold bars; only `candle_momentum` (37.8%) speaks there and contradicts itself across the two. Exness XAU/USD trades fine because `candle_momentum` carries the full 1.0 weight there and speaks decisively. Underlying design point: `strategy_weights` is one global number per strategy applied at every interval, but usefulness is interval-dependent — a strategy earns weight on fast intervals then carries that share into intervals where it is structurally silent, where the share is lost rather than deferring. **Fix tried and rejected:** renormalizing per interval across only non-`Hold` strategies (no-lookahead untouched, computed not stored). Two research images identical except the change, same instrument/window: 219→220 trades, PnL **−1.5353 → −1.5694** — no measurable gain, marginally negative, within noise. Fails the "validated out-of-sample" bar; reverted, not shipped. **Bigger finding: the backtest never freezes, so its 219 trades ARE this route's unfrozen behavior, and they return −1.54.** Frozen live it has lost −0.049 over 7 trades. The freeze has been protecting a negative-expectancy policy, not costing profit — so the real work is the edge, not the gate. Details → `raw/researcher/round203-xau-binance-freeze-diagnosed-fix-rejected-by-backtest.md`.

- **[trading][evidence][2026-08-25] Replay deadlock against the capacity-1 kline gate — ROOT CAUSE PROVEN AND FIXED, all 6 routes recovered, CLOSED:** a production goroutine dump (`/debug/pprof/goroutine?debug=2`) showed one stream blocked in `transport.(*writeQuota).get` holding the only gate slot while three more sat in `klineStreamGate.Acquire` for 7-8 minutes, with `stream_messages_total{sent}` frozen across 90s. Mechanism, confirmed by reading both sides: `open_replay_stream` ends with `replay.advance().await?`, so opening interval #2 blocks in the server's `Acquire` while #1 holds the slot for its whole send; the client, parked awaiting #2, never drains #1, whose handler then parks in flow control. Deterministic, not a race — which is why `finance-research` failed 7/7 at every window size, and why only high-volume intervals ever failed (`1d`/`12h` fit the initial flow-control window, so the server finished and released the slot; `5m`/`15m`/`30m` never did). Re-frames the whole saga: Round 141's "459-minute stuck Send" was this deadlock, and Round 142's `MaxConnectionAge` never fixed it — it broke the deadlock every 30 min by killing a stream, clearing the gate while guaranteeing no attempt could assemble all 8 intervals for `portfolio_inputs_complete`. **Fixed in finance-live-action `81dfcc1`:** collect each interval to completion one stream at a time, then merge in memory. Never more than one gate slot in use; mid-batch transport failure during the merge no longer exists. Merge sequence provably unchanged — `next_collected_index` applies the same `replay_order` over the same klines, pinned by two new tests (one asserts the in-memory merge reproduces sorting by `replay_order` exactly, one that equal close times still break by interval priority). No-lookahead ordering, evidence ordering, and `failed_intervals` semantics preserved. **Production verified:** all 6 routes `evidence=8/8`, `replay_scopes=3/3`; both BTC routes now hold full `1d` evidence (6/6, `2026-08-24T23:59:59.999Z`) and 3 committed paper-backtest replay scopes, up from 0. Full evidence → `raw/explain/kline-stream-gate-capacity-saga.md` Round 200.
  - **`MaxConnectionAge` removal (finance-mw `3f53612`) stays.** It was not the fix, but reverting would restore a 30-minute forced kill that guarantees replay can never complete. Keepalive (30s/10s) still covers dead peers. If a genuine flow-control stall ever recurs the correct fix is progress-based (abort/release the slot for a stream that has sent nothing for N minutes), never age-based.
  - **Both BTC routes are `protective_exit_waiting_for_fresh_insight` with `dsc=57`,** i.e. structurally able to trade again and waiting on a real signal — not stuck. Trades resume when the gate conditions are genuinely met.

- **[observability][evidence][2026-08-25] Deploy gate now asserts each route can actually TRADE, not just serve HTTP 200 — root fix for "green CI, broken production", DONE:** two regressions shipped green in one day (Round 142's `MaxConnectionAge`, Round 189's strategy addition), both invisible to the existing gate, which only checked HTTP 200 + deployed SHA + `/trading` returns HTML. Added three exported invariants from finance-live-action (`portfolio_evidence_intervals_complete`/`_required`, `portfolio_replay_scopes_completed`/`_expected`, `portfolio_decisions_total`) plus `scripts/verify-portfolio-readiness.sh`, wired into both `build-deploy.yaml` validation and the production verification workflow. Only evidence completeness hard-fails (the sole condition making `decide()` structurally impossible); replay counts are diagnostic. Counter lives in `RuntimeInner`, NOT `WorkerRuntimeState` — adding a checkpoint field is the exact change class that reset both BTC routes this morning. Covered by 7 stubbed shell cases + a `finance-core` unit test; no trading logic, evidence ordering, or no-lookahead behavior touched. Commits `10d9f07` + `fcec698` (the second fixes a false alarm the gate caught in its own logic when first run against production — Bybit BTC reported cannot_trade while demonstrably deciding). CI green, all 6 containers on exact SHA, gate verified live: 4 routes ready, exactly the 2 genuinely-broken BTC routes held.

- **[trading][high][2026-08-25] Binance BTC/USDT + Exness BTC/USD cannot produce ANY Portfolio decision — root cause found and fixed, awaiting replay to confirm recovery:** user asked why total trades were 0. Investigation (production-incident-debugging skill) found both routes at `evidence=2/8` with zero committed Portfolio replay scopes and zero decisions, while health/SHA/`evaluation_count` all looked normal. Causal chain, each step proven: Round 189's strategy addition changed the portfolio replay fingerprint → checkpoint's portfolio scopes invalidated → live evidence book reset to empty → `1d` interval held no evidence at all (next natural close 00:00 UTC) → `decide()` failed synchronization on every boundary → "Discarded expired Portfolio boundary at runtime" repeatedly. The book is normally re-seeded by the Portfolio replay, but that replay requires ALL 8 intervals to stream successfully in ONE attempt (`portfolio_inputs_complete`), each attempt takes ~63 minutes, and Round 142's `MaxConnectionAge=30m` guaranteed at least one interval died mid-batch every time (`h2 protocol error: error reading a body from connection` on 5m/15m/30m at 09:40/10:43/11:46 UTC) — a deterministic deadlock, not bad luck. **Round 142's commit message asserted the replay client tolerated mid-batch failures; that assertion was wrong** and is corrected in-code. Fixed by removing `MaxConnectionAge` entirely (finance-mw `3f53612`, deployed+verified) per owner direction to let keepalive ping/pong handle liveness instead of a blunt age limit. Residual risk accepted and documented: the narrow flow-control-stall case Round 141 saw is no longer covered; if it recurs the correct fix is progress-based (abort/release the gate slot for a stream that has sent nothing for N minutes), never age-based. **Still to confirm:** whether a full replay now completes and re-seeds both BTC routes — recheck `portfolio_evidence_intervals_complete` reaching 8/8 and `portfolio_replay_scopes_completed` > 0.

- **[trading][medium][2026-08-25] `finance-research` historical-replay requests deterministically stall the single-slot kline stream gate — 7/7 reproduction rate, needs Go-level investigation, not more CLI retries:** Rounds 190-196 tried 7 times across every window size (90-1825 days) and every gate baseline state (freshly recovered, idle, contended) — every single attempt froze `finance_mw_grpc_stream_messages_total{sent}` within ~15s of `requests_in_flight` going 4→5. `kline_service_server.go`'s `streamGate.Acquire()` (`capacity=1`, deliberately memory-constrained) explains the 4-baseline (3 production revision-recovery routes + steady queueing), but 100% reproducibility across 7 attempts rules out "spontaneous contention, bad luck" — something in how `finance-research`'s specific request (or the subsequent `repo.StreamEach` query) interacts with the gate appears to deterministically stall it. Every attempt was killed promptly (confirmed zero lasting production impact each time) but this blocks all ad-hoc historical backtesting via this tool until root-caused. Needs real Go-level investigation (stall logging inside `Acquire`/`StreamEach`, or reproduce via `grpcurl` outside the research CLI) — details and full attempt log → `raw/explain/kline-stream-gate-capacity-saga.md`'s Round 190-196 entries.

- **[trading][evidence][2026-08-25] LIVE incident: kline stream gate stuck, 3/4 production routes NOT evaluating — self-cleared within Round 142's MaxConnectionAge bound, CLOSED:** Round 193 found `finance_mw_grpc_requests_in_flight{method="Stream",service="kline.KlineService"}=4` with `stream_messages_total{direction="sent"}` frozen at `598897`, and Binance/XAU, Binance/BTC, and Exness/BTC all showing a frozen `evaluation_count` (Exness/XAU unaffected — different MT5 data path). Root cause not pinned to a specific trigger (consistent with the rest of `raw/explain/kline-stream-gate-capacity-saga.md`'s history — occurrences are spontaneous and haven't been traced to one client). **Round 194 re-check (~14 min later) confirms full recovery**: `sent` jumped `598897→700365`; `evaluation_count` resumed advancing on all 3 previously-frozen routes (XAU/binance `2132→2136`, BTC/binance `2130→2134`, Exness/BTC `6780→6784`). Recovered within Round 142's 30-min `MaxConnectionAge` bound as designed — no regression in that fix, no production mutation needed. Kept as evidence this bound continues working correctly in a real, freshly-observed occurrence, not just Round 142's own original test.

- **[observability][evidence][2026-08-25] Tempo tracing backend was OOM crash-looping (6100+ restarts, ~3-4/min) — found while tracing the kline-latency 502 per user request, fixed live + in repo, CLOSED:** `production-incident-debugging` skill run on the `/api/v1/observability/kline-latency` 502 (trace.id `fe76ea4c7f5488ab3c097e3150658050`) found tracing itself was non-functional: `docker events` showed continuous `oom→die→start` on `tempo-vc0gwk040csg4cwg88000k48` (RestartCount climbing live during the investigation, 5879→6119), Tempo's own logs showed `"partition not found in the ring"` on every restart (ring never converged between crashes), `GET .../api/traces/{id}` returned 500 `"empty ring"`, and otel-collector logs showed continuous `connection refused` to `finance-tempo:4317` — tracing had been fully non-functional for an unknown but clearly extended duration (finance-mw's own span export showed no errors, ruling out finance-mw as a contributing cause). Root cause: Tempo's 1G memory limit was insufficient to reload its ~6000-block compaction backlog on every restart. Fixed **live** via `docker update --memory=2g --memory-swap=2g tempo-vc0gwk040csg4cwg88000k48` (user-authorized SSH fix, applied without a container restart) — confirmed durable: RestartCount held flat at 6119 for 2+ minutes post-fix (was climbing every 15-20s before), memory settled at 434-454MiB/2GiB. **Verified tracing itself works again**: re-queried the exact same trace.id, got `200` with the full span (`finance-mw.http`, `http.response.status_code=502`, `url.path=/api/v1/observability/kline-latency`, `STATUS_CODE_ERROR`) — the original kline-latency 502 is now independently confirmed via trace, not just HTTP-access-log correlation. Repo fix (source of truth for future Coolify syncs) committed `48de2f7` — note Coolify's live compose (`docker_compose_raw` via its API) is a separate, independently-editable copy from this tracked YAML, so a future Coolify-triggered redeploy of the observability service could silently revert to 1G unless/until that service's stored compose is also reconciled to match (not yet done this round — flagged so a future redeploy of that specific Coolify service doesn't accidentally regress this).

- **[observability][medium][2026-08-25] Kline processing latency panel 502 since VM auth rotation (2026-08-21) — app-side fix shipped (commit `7ade99b`), credential wiring into production still open, needs a deliberate pass:** user spotted the chart page's red "Binance BTC: unavailable (status 502)" banner and asked to stabilize the data pipeline; investigation found the underlying chart/candle data is completely unaffected (`/api/v1/klines/sources` returns fresh live data) — only `/api/v1/observability/kline-latency` is broken. Root cause: `VictoriaMetricsClient` (`pkg/observability/victoriametrics.go`) never had any auth support; VM's `-httpAuth.username/password` was enabled/rotated 2026-08-21 (see the two `[observability][evidence][2026-08-21]` entries below) for Grafana/vmagent, but finance-mw's own query client was never included in that reconciliation, so every query has 401'd (surfaced as 502) for ~4 days unnoticed. Fixed the client to send optional Basic Auth via two new env vars (`VICTORIAMETRICS_QUERY_USERNAME`/`VICTORIAMETRICS_QUERY_PASSWORD`, empty = no auth = today's unchanged behavior) — safe to deploy as-is, zero behavior change until configured. **Not yet done: actually setting those two env vars in production to the same `finance-monitor` credential Grafana/vmagent already use.** Two real blockers found, not yet resolved: (1) `docker/env/production.env` is still git-tracked with real inline secret values (the same file already flagged in the `[observability][risk][2026-08-22]` credential-exposure entry below) — do not add a new secret there, it perpetuates a known anti-pattern already queued for remediation; (2) `scripts/coolify-deploy.sh`'s `reconcile_application_envs()` actively DELETES every non-`SERVICE_FQDN_`/`SERVICE_URL_` custom env key from finance-mw's Coolify application on every deploy, so a naive one-off `PATCH .../envs` would be wiped on the next deploy. The existing `scripts/rotate-victoriametrics-http-auth.sh` (`reconcile-victoriametrics-grafana-auth` mode) already has `old_username`/`old_password` in scope at exactly the point it updates Grafana's datasource — the natural, consistent extension is to also patch finance-mw's app envs there (Coolify app UUID `ftj9mknbxl7rljmwtoielnnb`, from `pipeline-ci-cd.yml`'s `COOLIFY_RESOURCE_ID`) through whatever durable-env mechanism finance-mw actually uses once (1) is understood, then verify `direct_vm_status`-style (200, not 401) from inside finance-mw's own container before declaring done. Needs its own careful, tested pass — not improvised inline with an unrelated round.

- **[trading][high][round 189] Swing 4h/1d BTC strategy DEPLOYED as an additional TREND-role strategy — Round 188's blocking risk was a misreading of the code, corrected before implementing:** Round 188 held off on registering `mtf_stochastic_14_3_30_70_sma50_trend_filtered` (3 independent confirmations, PF>1 every split — see Round 172-180 entry below) citing a "benefit-of-doubt" risk: a new strategy might carry full untested-strength trend-role influence for the ~1 year needed to accumulate 20 real trades. Round 189 re-read `mature_alpha_strategy_quality` (`trading_modes.rs:587-593`) and found it returns exactly `0.0` for any strategy below the 20-trade confidence threshold — the `1.0` benefit-of-doubt only applies to `alpha_performance_quality` (the *interval's* own aggregate weight, not the *strategy's*). A new strategy's actual score contribution (`interval_weight × strategy_weight × score`) is therefore exactly zero until it matures, the opposite of the risk as originally framed. Implemented: `mtf_stochastic_4h_1d_sma50` registered in `deployment_rules.rs::configured_extra_strategies` for both BTC venues (Binance perpetual, Exness CFD — matching the candidate's actual validation scope, not XAU). Updated 3 golden tests that hardcoded the old strategy count. Full local verification green (`cargo fmt --check`, `cargo test --workspace --exclude finance-redis`: finance-api 211/211, finance-core 78/78, finance-research 54/54, finance-strategy 87/87+130, 0 failures). Commit `fa23f16` pushed; CI/deploy/production-verification tracked as an addendum below once confirmed. Details → `raw/researcher/round189-swing-strategy-deployed-corrected-benefit-of-doubt-risk.md` (supersedes the "do not implement this round" conclusion in `raw/researcher/round188-architecture-proposal-swing-signal-as-trend-role-strategy.md`, which remains for its design-options record).
  - **Production verification confirmed a real gap, fixed same round (commit `2d8cfa7`):** immediately after `fa23f16` deployed, direct Redis checkpoint reads showed Binance BTC's live `strategy_weights` was MISSING `mtf_stochastic_4h_1d_sma50` entirely (5 keys, not 6), while Exness BTC had it (at `0.0`, correctly immature). Root cause: `restore_checkpoint_state` wholesale-replaces a freshly-built policy (which has every currently configured strategy) with whatever `strategy_weights` key set was persisted in the checkpoint — a strategy added to `deployment_rules.rs` after a route's checkpoint was last written stays permanently invisible to that route's live weights after a restart, unless the checkpoint happens to get invalidated for an unrelated reason (which is what let Exness BTC pick it up by chance). Not new to this deploy — structurally the same gap has applied to every extra strategy ever added to a long-running route. Fixed with `MultiTimeframePortfolioPolicy::sync_strategy_roster` (+ `MultiTimeframeEvidenceBook` pass-through), called unconditionally right after checkpoint restore with the currently configured strategy names; inserts any missing name at `0.0` (matching what `mature_alpha_strategy_quality` already computes for an unobserved strategy), never disturbs existing weights. New regression test added (`finance-core/tests/trading_modes.rs`). Full local `cargo test --workspace --exclude finance-redis` + `cargo fmt --check` green; commit `2d8cfa7` pushed, CI green (build-and-push/deploy-app both succeeded), all 6 containers verified on exact SHA `2d8cfa7` and healthy. **Re-verified in production after this fix: both Binance BTC and Exness BTC now show `mtf_stochastic_4h_1d_sma50` in `strategy_weights` at `0.0`, with every other strategy's weight unchanged** — confirms the fix works and the swing strategy is now genuinely live on both BTC routes, maturing normally toward its first real-performance reweight at 20 trades.
  - **Note: the `Production Live Action Verification` CI workflow failed on `fa23f16`'s deploy** (3rd occurrence of the known flaky pattern — see the separate `[observability][low][round 167-168]` entry) — independently confirmed a false alarm via direct SSH/Redis checks both immediately after and again after the follow-up `2d8cfa7` deploy.

- **[trading][high][round 183] XAU/binance stuck FLAT since ~2025-12-11 (69,610 decision cycles, ~241 days) — root cause identified as the exact mechanism Round 167 fixed, resolution NOT yet confirmed:** user directly spotted "last trade Sell 26/12/2025" in the frontend. Verified via Redis: `decisions_since_target_change=69610`, `reason: "protective_exit_waiting_for_fresh_insight"`. Root cause: before Round 167, XAU/binance's entry-role `interval_weight` (5m/15m/30m) was exactly 0.0, making `entry_score` mathematically exactly zero forever — not rare, impossible — so it could never clear `minimum_role_score` (0.10) to re-enter after its one protective exit. All 4 routes are in the identical mechanical state (flat, same reason); XAU/binance's is just far older because the other 3 have had more recent exits (they've kept trading; XAU/binance hasn't traded at all since). **As of ~5h post-Round-167-deploy (~50-60 cycles), XAU/binance has NOT yet re-entered — not proof the fix failed (too few cycles to expect a strong-enough signal alignment at the small 0.05 floor weight), but not proof it worked either.** Round 184: computed the LIVE `entry_score` by hand from real per-interval evidence = **-0.01195** (needs |score|≥0.10) — `candle_momentum`/`rsi_mean_reversion` are on opposite sides at all 3 entry intervals right now (structural anti-correlation, not a fluke). Best-case ceiling at floor=0.05 (both strategies agreeing all 3 entry intervals) ≈ **0.144**, which does clear 0.10 — so the fix is mathematically capable of working, just needs a genuine directional-agreement moment between the two strategies, which may take real time. Re-check `decisions_since_target_change` for all 4 routes in 1-2 days: if XAU/binance's has reset (small number), the fix worked; if still climbing with no sign of even partial 2-of-3 agreement ever occurring, `INTERVAL_QUALITY_FLOOR=0.05` needs raising with the same real-data-simulation discipline as Round 165-167. **Round 189 re-check:** only ~19-30 more cycles elapsed since the last read (XAU/binance 69,610→69,629, BTC/binance 206→224) — still stuck, but far short of the "1-2 days" needed for a real verdict; both Exness routes have cleanly exited/re-entered in the meantime (no regression). **Round 191-192 correction — found the real current blocker:** Round 184's `entry_score` calculation was only half the gate; re-reading `MultiTimeframeEvidenceBook::decide()` shows the gate ALSO independently requires `trend_score.abs() >= 0.10` (plus sign agreement with `entry_score`). Live entry_score is `+0.1436` (clears, both strategies long across 5m/15m/30m, persisting 10+ minutes across 2 checks) but live trend_score is only `+0.0686` (does not clear) — 12h/1d strongly long, but 4h strongly short and 1h/2h roughly cancel. This is the real, current, precise blocker, corrected from the prior entry-score-only ceiling estimate. Details → `raw/researcher/round183-xau-binance-8-month-stall-explained-by-round167-fix.md`.

- **[trading][evidence][round 181-187] BTC 5m RSI entries gated by 1d SMA trend filter — DEFINITIVELY CONFIRMED real but MODEST edge, second genuine candidate, not promoted:** `mtf_rsi_14_20_80_trend_filtered`/`mtf_rsi_14_30_70_trend_filtered` on BTC — confirmed across 2 windows + cross-broker (Exness near-identical to Binance) at PF 1.02-2.72 on small samples (17-45 trades). **Round 187 full 5-year window (130/37/43 trades, the definitive sample size) recalibrates honestly: PF 1.01-1.13/1.02-1.06/1.38-1.54** — still positive every split, edge is real, but far more modest than the small-window readings suggested (small-sample inflation, not wrong direction). Second genuine candidate alongside the swing signal (Round 172-180), but smaller effect size — less obviously worth the same architecture investment. Same open question applies (how to fit into Portfolio construction), lower priority than the swing candidate given the modest magnitude. Details → `raw/researcher/round181-5m-1d-trend-filter-mixed-results.md`.

- **[trading][high][round 172-180] `mtf_stochastic_14_3_30_70_sma50_trend_filtered` (swing 4h/1d BTC) DEFINITIVELY RE-CONFIRMED real (3 independent measurements agree), still not promotable — real next step is an architecture decision, not more backtesting:** full 5-year Binance re-run (same 39/19/18 trade counts as the original 2026-08-20 measurement) gives PF 1.58/2.43/1.50 — close match to the original pre-bugfix numbers (1.66/2.04/1.65), no collapse like the 7 other MTF strategies Round 67 found tainted by the lookahead bug. Combined with the 2-year cross-broker check (Binance 2.62/3.54/3.63, Exness 2.60/3.47/3.52, near-identical), this candidate now has **3 independent confirmations** (original pre-bugfix 5yr, fresh post-bugfix 2yr both brokers, fresh post-bugfix full 5yr) all agreeing PF>1 every split. This is the only candidate in the program's history with a real, this-thoroughly-confirmed positive-PF edge. Still blocked from promotion by 2 unchanged, real flaws: ~48-day max losing streak (limit 5) and ~0.35 trades/week (target 7/week) — not independently re-verified this round (no per-trade fill data in this JSON output to reconstruct the streak, but win rates 33-53% with PF>1 across splits is consistent with the trend-following mechanism, not a red flag). Real next step is an architecture decision (low-frequency signal as a separate Portfolio bucket, or a trend-bias/gate role for the 5m signals — see Round 17's Todo item), not more backtesting — the edge question is closed. Details → `raw/researcher/round172-swing-mtf-stochastic-revalidated-post-bugfix.md`.

- **[observability][low][round 167-168] `Production Live Action Verification` workflow: recurring transient failure pattern, retry budget exhausts before deploy fully rolls over — 3rd occurrence observed this session (round 189):** three times now (commits `2243e19`, `7fe0e13`, and `fa23f16`), the `verify` job's 12-attempt probe loop shows `status=200` for `/healthz`/`/api/v1/health`/`/api/v1/system/version` on every single attempt, yet the job still fails with `production_live_action_verification=failed`. All three times, an independent manual check shortly after (direct SSH `docker ps` + Redis checkpoint read) confirmed production genuinely healthy on the exact expected SHA — so this is the CI check's own retry budget (~2 min, 12×10s) being too tight for how long a real rollout across 6 containers takes sometimes, not an actual production problem. Not urgent (production evidence has been independently confirmed all three times) but worth a look: either widen the retry budget/count, or have the probe assert against the deployed SHA explicitly rather than just body-shape, so a future genuine mismatch isn't masked by "well it was flaky last time too."

- **[trading][low][round 162] `finance-research --daily-profit-gate` silently ignores `--portfolio-minimum-hold-decisions` — real tooling gotcha, verified by code + experiment:** `finance-api/src/config.rs:110` hardcodes the compiled `DEFAULT_PORTFOLIO_MINIMUM_HOLD_DECISIONS` constant with no override path reachable from `--daily-profit-gate`; confirmed empirically (hold=12 vs hold=36 at identical stop/take gave byte-identical output: PnL -13.33, 1889 trades, Sharpe -6.64 both). Not a correctness bug (production always uses the real compiled default, so live behavior is unaffected) but a footgun for future research rounds trying to sweep this parameter via the gate tool — should either wire the override through, or add a clap `conflicts_with`/loud warning so a future round doesn't waste time like this one did. Low priority since it doesn't affect production. Details → `raw/researcher/SUMMARY-priority-backlog.md` §Rule 1 Round 162 addendum.

- **[observability][low][2026-08-23] Scheduled `Coolify Resource Maintenance` workflow failing (`enforce-elasticsearch-log-retention` mode) — unrelated to Round 121's Bybit fix, noticed in passing while tracking CI:** run `32624449081` (07:01:13Z schedule trigger) failed with `chown: cannot access '/data/es/filebeat/config/config.yml': No such file or directory` and the same for `ilm-policy.json`, exit code 1. Not investigated further this round (out of scope, no production trading impact observed). Needs a look next round: likely a runner path/mount drift for the filebeat config the retention-enforcement step expects to `chown`.

- **[trading][evidence][round 120-124] Bybit `5m` kline WS crash-loop — CLOSED, fixed at source, downstream catch-up confirmed complete:** root cause + fix + resolution → `raw/explain/bybit-5m-kline-ws-crash-loop.md`. Same round also discovered the `KlineService/Stream` 1-slot gate (contention during catch-up) — see `raw/explain/kline-stream-gate-capacity-saga.md`.

- **[trading][evidence][2026-08-23] Elder Ray Index (EMA+high/low combined, genuinely new construction) — CLOSED, same failure mode as round113's OBV:** new `ElderRayStrategy` (net power = bull_power+bear_power crossing zero). Win rates 7.1-14.5% and very high signal frequency (up to 47,494 trades on BTC/binance train) — same overfitting-to-5m-noise signature as OBV. PF 0.097-0.348 across all 12 cells, BTC cross-broker consistent (0.335/0.296/0.284 vs 0.348/0.323/0.295). No cell near breakeven, no 18mo check needed. No promotion. Details → `raw/researcher/round119-elder-ray-closed-same-failure-mode-as-obv.md`.

- **[trading][evidence][2026-08-23] Extended round78's `ema_crossover_12_26` anchor interval sweep to 1h (previously only 15m/30m tested) — no code change, research-only:** BTC stable and cross-broker near-identical at 1h (binance 0.861/0.892/0.825, exness 0.853/0.874/0.851) — extends the "stable anchor" evidence to a second interval. XAU/binance showed train=1.125 and holdout=1.053 both >1 but validation dipped to 0.652 on a thin 38-112 trade sample, and XAU/exness showed a completely different shape at the same interval — self-disqualifying zigzag, no 18mo cross-check needed (same discipline as round107/114). No promotion. Details → `raw/researcher/round118-ema-crossover-1h-interval-sweep-extension.md`.

- **[trading][evidence][2026-08-23] Three White Soldiers/Black Crows (3-candle continuation, genuinely new geometry vs round103's 2-candle Engulfing) — CLOSED:** new `ThreeCandleContinuationStrategy` (no parameters to sweep). All 12 cells PF 0.280-0.630, no cell near breakeven, no 18mo check needed. Best cell BTC/exness (0.605-0.630). Same failure mode as Engulfing — pure candlestick-geometry patterns without a volume/oscillator component keep failing in this program. No promotion. Details → `raw/researcher/round117-three-candle-continuation-closed.md`.

- **[trading][evidence][2026-08-23] MFI (Money Flow Index) mean reversion (new volume-weighted bounded 0-100 oscillator, distinct from OBV/RSI) — CLOSED both variants:** added `finance_strategy::indicators::mfi` (new file, 4 unit tests) and `MfiReversionStrategy` (14/20/80, standard thresholds). Bare variant: PF 0.387-0.958, win rates 33.8-58.2% — notably higher win rate than most candidates this session, and BTC/exness validation (0.958, n=460) is the closest any bare/unfiltered oscillator has come to breakeven in the whole program. Still <1 everywhere, no promotion. ADX-range-filtered variant (matches mean-reversion framing better than an SMA trend filter): thins samples to 12-269 trades and produces inconsistent zigzag shapes across routes, including validation-split spikes to PF 1.475-3.640 on 12-41 trades — classic thin-sample artifacts, self-disqualifying within the 5yr window so no 18mo cross-check was needed (evidence already too unstable, unlike round106's cross-broker-consistent near-miss). No promotion either variant. Details → `raw/researcher/round114-mfi-reversion-closest-bare-oscillator-adx-filter-too-thin.md`.
  - **Concurrent-writer guard 12:54-12:59 UTC+7:** Codex đã mở isolated subagent theo Todo nhưng dừng ngay trước edit/commit khi phát hiện checkout chính FLA có đúng code MFI chưa commit trên HEAD exact `7d85cdb5d10c868726c90fa27ee808287a99cb5d`: modified `finance-research/src/strategies.rs`, `finance-strategy/src/indicators.rs`, untracked `indicators/mfi.rs`; mtimes lần lượt 12:49:35, 12:48:02 và 12:19:01 UTC+7. Đây là bằng chứng writer Claude đang tiếp tục cùng task trong lúc Codex còn active, trái giả định isolated ownership; Codex không đọc diff/không đè/cherry-pick, tạm dừng bắt đầu task mới và chờ owner/Claude xác nhận qua handoff ai sở hữu Round114. Pipeline FLA `7d85cdb5`/MW `7b2a603` đã push trước bằng chứng này vẫn chỉ được monitor tiếp.

- **[trading][evidence][2026-08-23] BTC/binance + XAU/binance workers panic-restarted at 03:08 UTC — confirmed fail-closed by design, not a bug:** both binance live-action workers had `RestartCount=1`, ~1h uptime vs Exness's stable 12h. Logs show the authoritative mark-price gRPC dependency failed past retry budget, triggering the intentional `panic!("fatal worker data-contract failure: {error}")` at `finance-api/src/main.rs:1274` (verified against the exact deployed source at `56437f2d93c0c47f76e936b0a237ec72baa5a6b6`) — Docker restarted cleanly, checkpoint state in Redis was unaffected (no reset, no gap). Root cause: `finance-mw-1` itself had the same ~58min uptime, confirming an MW redeploy (likely `dfa4b82`/`5aa24df`) briefly interrupted gRPC right when the binance workers' mark-price call hit it. No action needed — this is evidence the fail-closed design works correctly during a real deploy event, not a synthetic test. Details → `raw/researcher/round111-production-health-mark-price-panic-restart-confirmed-by-design.md`.
  - **Round 112 follow-up — refined, see new Todo item below:** dug into exactly why this specific error hit the fatal path instead of the `af09209` "keep workers alive" retry path. Found a possible narrow classification gap in `FundingSourceError::is_retryable()` — logged as its own actionable item rather than leaving this evidence entry's "not a bug" framing unqualified.

- **[trading][low][2026-08-14] Portfolio showed multiple entries inside a single 12h BTC candle (2026-02-23) — investigated, real cause found, but the re-entry cooldown/circuit-breaker fix is still an open design decision:** found orphaned during a `raw/` cleanup audit. Details → `raw/explain/portfolio-multi-entry-per-candle.md`.
  - **Codex audit 21:20-21:24 UTC+7:** phần hiển thị đã đóng bởi MW `95db96f` (gom marker cùng candle/side thành `N×`, web regression 18/18 xanh), còn runtime không có invariant “một entry/12h” vì ledger tick primary 5m và 12h chỉ là evidence. Round80 `efe7854` chỉ validate reversal hold 36 candles; explicit exit reset về Flat vẫn cho same-direction initial entry, protective exit đã có wait regression. Không implement cooldown mới vì chưa có policy semantics (loại exit/direction/threshold/reset) và chưa có train/validation/final-holdout + cross-broker evidence; tự chọn có thể làm giảm Make Decision. Giữ Todo như research prerequisite, không còn coi hiện tượng UI hay nhiều marker 12h là execution bug.

- **[trading][low][2026-08-21] Grid trading (TP/SL/Entry ladder) — never attempted in this program:** would need a real architecture change (`PortfolioConstructionState::construct()` only targets one position at a time) and conflicts with the program's core finding that pure mean-reversion has no edge without a trend filter. Proposed cheap first probe: simulate tiered TP/SL around the current entry instead of true parallel grid orders. Not started.
  - **Codex queue review 23:47 UTC+7:** không mở multi-order architecture khi chưa có edge; bước hợp lệ tiếp theo là Quant implement tiered-exit probe research-only qua `one_target`, train/validation/final-holdout + cross-broker. Backend implementation phụ thuộc probe đó pass và phải định nghĩa order/ledger/risk semantics, nên giữ Todo phía research thay vì merge branch suy đoán.

- **[trading][medium][round 17] Swing 4h/1d family as a trend bias/gate for the live 5m signals, not a standalone entry — architecture proposal, not implemented:** 4 candidates at 4h/1d base all hit the same frequency-vs-quality ceiling; best (ADX-gated) has Sharpe 1.37/Sortino 4.61 but only 0.21 trades/week. Confirmed naively lowering the base interval doesn't scale frequency the way expected. **This is now the leading candidate use-case for `mtf_stochastic_14_3_30_70_sma50_trend_filtered` (round 172-180 Done entry above) — a definitively-confirmed real edge blocked on the exact same frequency ceiling.** Details → `raw/researcher/round17-swing-4h1d-regime-filter-family.md`, `round19-mtf-frequency-bottleneck-mechanism.md`, `round35-losing-streak-dates-pinpointed.md`.
  - **Codex queue review 23:47 UTC+7:** standalone fail Target3 và “bias/gate” chưa có exact combination/holdout semantics qua Portfolio engine thật; không tự chọn gate vì có thể giảm Make Decision trong lúc margin hiện chỉ 7.2-7.3/week. Cần Quant trả exact rule + `one_target` evidence trước khi có backend task.

- **[observability][low][n/a] `1m` collection is disabled, không phải retention chỉ ~4.4 ngày — blocks honest walk-forward backtesting of any 1m/intrabar-structure strategy (RSI scalping, FVG/ICT, liquidity sweep):** audit source xác nhận Timescale chỉ có một policy toàn bảng `drop_after=5 years`; 6,353 nến quan sát trước đây là remnant không tăng vì commit `33a4ca0` đã loại `1m` khỏi cả active WS và historical backfill intervals. Re-enable 1m cần owner chọn routes/horizon: 5 năm ước tính ~0.51 GiB/route (2 Binance ~1.02 GiB; 4 routes ~2.04 GiB trước overhead/WAL/backup), tăng khoảng 3.02× tổng rows hiện tại; Exness còn `MT5_MAX_BARS=600,000` chỉ đủ ~416.7 ngày thay vì 2,635,200 bar/5 năm. Retention 2/2, Go marketdata/services/worker tests xanh; chưa code vì không có target owner-approved và FVG chưa có holdout edge. Production re-query vòng này chờ network/SSH hồi; last authoritative archive đã xác nhận policy 5 năm.

- **[trading][medium][n/a] Swing 4h/1d MTF stochastic — first candidate in the program with positive Sharpe AND Sortino AND consistent PF>1 across all 3 splits, cross-broker validated, but blocked on 2 real gate fails:** 48-day losing streak (limit 5) and 0.35 trades/week (target 7/week). Needs either a regime filter for the streak or accepting swing as a separate low-frequency target bucket. **RE-VALIDATED Round 172-179, see the round172 entry above** — see `raw/researcher/portfolio-btc-optimization-log.md` (same-day entry) for original context.
  - **Codex queue review 23:47 UTC+7:** không promote candidate đang fail cả streak và frequency; “separate low-frequency bucket” là business/risk policy mới cần owner/Quant định nghĩa capital allocation, loss limit và dashboard scope. Chưa có implementation an toàn để giao.

## Dev-done

- **[observability][high][2026-08-27] Tách Finance Web khỏi finance-mw — local cutover hoàn tất, chờ owner mở push gate:** `finance-web` commits `6eaf2b7`, `7fd7dc1`, `b957937`, `49d6777` chứa source, standalone Makefile/README, Compose/verifier và workflow CI/CD; 50 test files/328 tests, lint, build, verifier tests và Compose config đều pass local. `finance-mw` commits `ad43acd` + `d450876` xoá `web/`, web-owned Compose/script/workflow và các job publish/deploy web khỏi pipeline MW, đồng thời đổi tên gate protobuf thành `live-action-data-contract`; Go test/vet/build, shell contracts, workflow YAML parse xanh. Chưa push/CI/Coolify/production; sau khi owner mở gate cần push cả hai repo, theo dõi CI standalone + cutover Coolify rồi chuyển Verify để Claude kiểm tra độc lập.

- **[trading][high][2026-08-27] Nối durable trade log vào đúng Portfolio closed-ledger — local gate xanh, chờ owner mở push gate:** finance-live-action commit local `e452083716e39f2a1d7a7caa8cb9a1186fcf8df1` trên base `4bce285` lấy closed trade từ ba đường realtime Portfolio (target/reversal/protective, dependency-degraded protective, authoritative mark liquidation), chặn cứng Alpha + Backtest/historical replay tại context boundary, và ghi typed payload vào exact ZSET `trades:<broker>.<market_type>.<base>.<quote>`; trade batch + worker checkpoint cùng commit atomic bằng Lua trước Kafka ack để đóng crash-window mark-price, stable event ID retry no-op và conflicting payload fail-closed. Local fmt, finance-redis/core/api tests, clippy, release build và Redis persistence replay đều xanh (`PASS_1_ZCARD=1`, `PASS_2_ZCARD=1`, checkpoint `42`, key XAU exact). Chưa push/CI/Coolify/production; `trade_publisher.rs`, TradeService gRPC và ba bảng Postgres chỉ xoá sau khi owner/Claude review phần Portfolio/replay.

- **[observability][low][2026-08-27] Dọn lại docs trong finance-workspace — LOCAL GATE XANH, ĐÃ COMMIT LOCAL/CẤM PUSH:** đã archive 7 notes và 4 dashboard plans/spec đã supersede; đưa trading decision pipeline lên `docs/specs/trading-decision-pipeline.md`; giữ lại 6 runbook, observability spec và diagram; chuẩn hóa link code sang `finance-mw`/`finance-web`/`finance-live-action` trên GitHub và loại path máy cũ khỏi active docs. `git diff --check` và kiểm tra link nội bộ chính đều xanh. Commit `d1b3b7e`; chưa push/CI/Coolify/deploy theo owner-held push gate.

**Round 135 review note (2026-08-24):** every entry below is an isolated local commit on Codex's own (never-pushed) checkout, held back pending the XAU marker-backfill lane that closed back in Round 115 (2026-08-23) — Codex's session ended before combining/pushing them, and with Codex now out of quota there's no access to that local branch state to verify diffs directly. Not urgent to recover: every one is a CLOSED, research-only, non-promoted candidate (PF<1, no runtime impact either way), and every conclusion is already preserved in `raw/researcher/SUMMARY-priority-backlog.md`'s closed-directions table (Round 88/91/93/99-100/107/108/109/110/113 all present). The only loss from these commits never landing is the literal reproducible code for already-closed candidates — not a functional or research gap. No action taken this round; flagging so future rounds don't re-investigate assuming these are actionable.

- **[trading][low][2026-08-23] OBV trend Round113 — CLOSED và implementation research-only đã sạch:** combined FLA exact head `7d85cdb5d10c868726c90fa27ee808287a99cb5d` trên parent Bybit/funding/Round110 `c2220489e9bcdaf6b89713ff1c80a04ae156908b`, chỉ thêm cumulative signed-volume OBV/SMA20 one-shot cross, state instrument+timeframe, replay identity, đúng một candidate và non-promotion regression. TDD đỏ đúng missing type; Docker `--cpus=3` hard-timeout: targeted 4/4, finance-research 123/123, workspace ngoài Redis integration, fmt, release build 5m53s, clippy exit0 (baseline warning) đều xanh; root review diff/logic/attribution và fast-forward combined đạt. Audit sửa claim material: Round72 taker imbalance đã là volume-derived primary signal, nên OBV chỉ là candidate **cumulative signed total-volume** đầu tiên; cross-broker cùng hướng không chứng minh “real signal”, report thiếu immutable dataset/report fingerprint/exact splits/image/cost và replay-start/warmup là input material. Chưa push/deploy ở thời điểm ghi entry; sẽ giao ngay trên final combined SHA. Details → `raw/researcher/round113-obv-trend-closed-weakest-candidate-this-session.md`.

- **[trading][medium][2026-08-23] Round112 funding transport retry — regression exact production shape đã sạch:** combined FLA commit `8a20cb0` (cherry-pick source `d72f5d79a0db2f944c67317e8a918f950f4ae914`) chỉ đổi `finance-api/funding.rs`: `Unknown/"transport error"` chỉ retry khi `Status::source()` thực sự là `tonic::transport::Error`; server/poison `Unknown` cùng message nhưng không có local transport source vẫn fatal. Regression dựng tonic server + TCP proxy rồi ngắt RPC mark-price in-flight để tái hiện exact shape, poison case đối chứng và deadline 5 giây bounded; root review logic đạt, targeted 3/3 xanh sau timeout review. Docker `--cpus=3`: fmt, finance-api 211 unit + 7 contract, workspace ngoài Redis integration, workspace build đều xanh; full workspace chỉ fail 9 Redis test vì thiếu Docker lồng, strict clippy chỉ debt có sẵn. Chưa push/deploy để giữ exact XAU pin; sẽ giao trong combined chain và verify 6 worker không restart/panic sau MW rollout. Details → `raw/researcher/round111-production-health-mark-price-panic-restart-confirmed-by-design.md`.

- **[trading][low][2026-08-23] CCI momentum breakout Round110 — CLOSED và implementation research-only đã sạch:** combined FLA commit `01a795875d9616ca73b7b7c3234f7d7ea29ee5a5` trên exact Round109 `1fd8ce658cf633f2a67a0abb19a1c0350fedd2b9`, thêm CCI generic + 5 unit test, hai candidate raw/SMA10, state tách instrument+timeframe, replay identity và regression crossing/no-repeat/non-promotion. Root review logic/diff/attribution đạt và fast-forward combined head; Docker `--cpus=3` + hard timeout: `finance-strategy` 78/78, `finance-research` 119/119, fmt, clippy mặc định, dev/release build đều xanh; workspace chỉ fail 9 Redis integration do container không có Docker lồng, strict clippy chỉ vướng 3 debt ngoài diff. Report thiếu immutable dataset/report fingerprint, exact split/image/cost và “SMA gần no-op” chưa có correlation measure, nên giữ historical closure; chưa push/deploy để giữ exact XAU pin, sẽ giao trong combined chain sau marker backfill. Details → `raw/researcher/round110-cci-breakout-closed-trend-filter-was-a-no-op.md`.

- **[trading][low][2026-08-23] Parabolic SAR Round109 — CLOSED và implementation research-only đã sạch:** combined FLA commit `1fd8ce658cf633f2a67a0abb19a1c0350fedd2b9` trên exact Round108 `fc86c7739338eca436ef15bd66c98e99c3fc8174`, chỉ đổi `finance-research`: standard AF `0.02/0.02/0.2`, state tách instrument+timeframe, registry/replay identity và hai regression đối xứng bắt đúng bug clamp current candle bằng cách chỉ clamp previous high/low. Root review logic/diff/attribution đạt và fast-forward combined head; Docker `--cpus=3` + hard timeout: targeted 3/3, full `finance-research` 115/115, fmt, clippy mặc định và release build đều xanh; strict `-D warnings` chỉ vướng debt ngoài diff. Report thiếu immutable dataset/report fingerprint/image digest nên PF là historical closure, không bit-for-bit replay; chưa push/deploy để giữ exact XAU pin, sẽ giao trong combined chain sau marker backfill. Details → `raw/researcher/round109-parabolic-sar-closed-self-caught-clamp-bug.md`.

- **[trading][low][2026-08-23] Ichimoku Cloud TK-cross + displaced cloud filter Round108 — CLOSED và implementation research-only đã sạch:** new `IchimokuCloudStrategy`, standard 9/26/52/26 periods, cloud filter intrinsic to the system rather than a bolted-on wrapper. All 12 cells PF<1 (0.507-0.881); BTC có shape ổn định nhưng không cell nào hòa vốn, XAU yếu/cross-broker inconsistent nên không promotion. Isolated commit `fc86c7739338eca436ef15bd66c98e99c3fc8174` có parent exact Round107 `6e28dc5fe36016d7f191b1e6118fce7920b110c0`, chỉ đổi `finance-research`: rolling midpoint high/low, đọc cloud displaced trước khi push state hiện tại, one-shot TK cross xác nhận close trên/dưới cloud, state tách instrument+timeframe và candidate duy nhất `ichimoku_cloud_9_26_52_26`. Root review + fast-forward combined linear head, `git diff --check`, Docker `rust:1.88-slim-bookworm --cpus=3` với hard timeout: fmt xanh, full `finance-research` 112/112, build xanh (chỉ warning `selectable` dead-code có sẵn). Report chưa có immutable candle/report fingerprint nên PF chỉ là historical evidence; chưa push/deploy để giữ exact FLA pin cho XAU, sẽ giao cùng combined chain sau marker backfill. Details → `raw/researcher/round108-ichimoku-cloud-closed-second-stable-anchor.md`.

- **[trading][low][2026-08-23] Volume filter Round107 trên `ema_crossover_12_26`@30m — CLOSED và implementation local đã sạch:** FLA commit `6e28dc5fe36016d7f191b1e6118fce7920b110c0` trên exact Round106 docs head `70210debafb4818614589047df229c5d67a0a88e` chỉ thêm candidate research-only `ema_crossover_12_26_vol20_1_3`, 4-route permanent caveat và replay-identity regression; không promotion. Root review xác nhận report thiếu immutable candle/report fingerprint nên PF chỉ là historical evidence; combined linear head đã fast-forward tới commit này. Docker `--cpus=3` + hard timeout, fmt + full `finance-research` 108/108 + build xanh; chưa push/deploy để giữ exact FLA pin cho XAU, sẽ giao cùng combined chain ngay sau marker backfill. Details → `raw/researcher/round107-volume-filter-on-ema-crossover-anchor-still-noisy.md`.

- **[trading][low][2026-08-23] Signal-strength filter Round99/100 đã CLOSED và implementation research-only đã sạch:** isolated FLA commit `5aeaeb6767f13eef866ab76da1ae7981aabed542` trên combined head `9c1fbb3` thêm stateless `MinStrengthFilterStrategy`, đúng sáu candidate prefix `min_strength_0_X_...`, permanent thin-sample/Keltner-no-op caveat và regression inclusive threshold/replay/registry collision; không chạm `StrategyKind`/deployment rules. Docker CPU3 fmt, targeted, full `finance-research` 95/95, build và diff-check đều xanh; root review diff/attribution đạt, chưa push/deploy để giữ XAU exact lane, sẽ nằm trong combined FLA chain sau khi marker backfill đóng. Details → `raw/researcher/round99-strength-filter-closed-thin-sample-overfit.md`.

- **[trading][low][2026-08-22] Round94 trend-filter trên Keltner/Donchian đã CLOSED, cần lưu research candidate:** Keltner MTF-filtered vẫn PF<1 toàn bộ; Donchian gần PF1 trên cửa sổ 5 năm nhưng đảo hình dạng hoàn toàn ở cửa sổ 18 tháng độc lập, nên không promote runtime. Details → `raw/researcher/round94-trend-filter-on-closed-mean-reversion-breakout-still-closed.md`.
  - **Implementation 23:58-00:10 UTC+7:** isolated commit `5d46af64b47f0821b9c162b04112d5a4b1441ae7` trên exact `2840dcc` chỉ đăng ký Keltner(20,2.5)+SMA10 và Donchian(200)+SMA10 research-only; TDD exact names/wrapper semantics đỏ→xanh, Docker `--cpus=3` fmt + full `finance-research` 90/90 + build xanh. No-lookahead an toàn nhưng exact close-time tie dùng higher-TF cũ thêm một base candle so với production barrier; JSON semantics chưa chứa inner params nên label/test là contract. Đã cherry-pick vào combined Round92/93 head `5c7b30be64aa13a9b7315e7724b6806ab3f635eb`; combined Docker targeted Round93 3/3 + Round94 1/1 và fmt xanh. Chưa push/deploy để giữ FLA production pin cho XAU lane.

- **[trading][low][2026-08-22] Heikin-Ashi smoothed momentum Round93 added to research grid and CLOSED:** confirm-candles 1/3/5/10 đều PF<1 trên 4 route; giảm tần suất làm PF tiến về khoảng 0.65-0.71 nhưng không vượt 1, cùng plateau với Donchian/Keltner. Chỉ là research candidate, không promote runtime. Details → `raw/researcher/round93-heikin-ashi-momentum-closed-plus-plateau-ceiling-pattern.md`.
  - **Methodology caveat 23:48 UTC+7:** kết luận CLOSED vẫn hợp lệ vì toàn bộ cells PF<1, nhưng diễn giải “trần PF do chi phí cấu trúc ~7bps/lệnh” chưa được chứng minh: fee 5bps + slippage 2bps áp trên từng fill nên round-trip nominal là 14bps trước spread/funding, và report chưa có gross/no-cost/cost-stress ablation để tách causal cost ceiling khỏi negative raw edge. Giữ đây là hypothesis cho round sau, không ghi thành invariant production.
  - **Ablation Round 96 (2026-08-23):** chạy lại 3 candidate tốt nhất với `--fee-bps 0 --slippage-bps 0`. Xác nhận PHẦN LỚN giả thuyết, có sắc thái: `keltner_reversion_20_2_5` và `heikin_ashi_momentum_10` gần như chạm đúng PF≈1.0 khi bỏ chi phí (từ 0.65-0.78 lên 0.91-1.06) — "cost-limited", raw edge gần 0, không âm. `donchian_breakout_200` vẫn dưới 1 (0.96) dù bỏ hết chi phí — "edge-limited", có khoản edge âm thật nhỏ ngoài chi phí. Không có candidate nào promote được (chưa đủ margin dù đã bỏ phí), nhưng lần đầu phân loại được cost-limited vs edge-limited thay vì gộp chung "PF<1, đóng". Details → `raw/researcher/round96-cost-ablation-confirms-structural-ceiling-hypothesis.md`.
  - **XAU completion (Round 98):** filled in the 2 XAU routes Round 96 didn't cover (Codex's own permanent comment noted it was "BTC-only"). Same cost-driven direction (all 6 cells rise substantially with fee=0/slippage=0, several cross 1.0: donchian XAU/exness 1.36, keltner XAU/binance 1.08, heikin_ashi XAU/exness 1.10) but does NOT change any CLOSED conclusion — XAU/binance is thin-sample (~85 days) and XAU/exness has known unverified continuity gaps, both already-documented reasons this program treats XAU evidence as non-promotion-grade regardless of PF value. Details → `raw/researcher/round98-cost-ablation-xau-completion.md`.
  - **Research record 00:30-00:31 UTC+7:** combined FLA commit `c6f3d85` đã ghi đúng BTC-only ablation vào permanent comments: Keltner/Heikin-Ashi cost-limited nhưng không có margin cho chi phí thật; Donchian vẫn 0.96 no-cost nên edge-limited. Chỉ docs, Docker CPU2 fmt và diff-check xanh; combined local head nay `c6f3d85`, chưa push/deploy để giữ XAU exact pin.
  - **XAU ablation record 08:41-08:43 UTC+7:** combined FLA head mới `9c1fbb35acabaeafe8d3f9f33897b92289fa9ab8` bổ sung permanent comments Round98 đúng 6 holdout PF no-cost (Donchian `1.03/1.36`, Keltner `1.08/0.80`, Heikin-Ashi `0.74/1.10`, Binance/Exness) và giữ rõ CLOSED/research-only/no-promotion do Binance history ngắn + Exness continuity chưa verify. Root review diff/commit attribution/worktree sạch; Docker `--cpus=3` fmt + full `finance-research` 93/93 xanh. Chưa push/deploy để giữ XAU exact pin; checkout Claude dirty không bị chạm.
  - **Implementation 23:54-23:56 UTC+7:** FLA isolated commit `a5317ac26354c135cd01f22830a0529ec895d395` trên exact `2840dcc` chỉ thêm candidate research-only, state tách theo instrument+timeframe, recurrence HA từ candle trước và exact grid; ba regression targeted xanh, full `finance-research` 92/92, build/fmt/diff xanh trong Docker `--cpus=3` với hard timeout. Đã cherry-pick sau docs Round92 thành combined local head `b599a43`, chưa push/deploy để không làm stale XAU exact lane; checkout Claude dirty không bị chạm.

- **[trading][medium][2026-08-22] Target 3 (≥7 trades/week) frequency margin is much thinner than documented — no more headroom to extend `minimum_hold_decisions`:** corrected `one_target` evidence gives current hold36 + stop/take0.01/0.02 about 9.3/week over 5 years and only 7.2-7.3/week over 18 months; hold72 already fails the 7/week gate on the recent window, so no further hold extension is safe. Details → `raw/researcher/round92-hold-extension-closed-target3-margin-thinner-than-documented.md`.
  - **Docs correction 23:35-23:38 UTC+7:** stale Round80 paragraph trong `raw/researcher/SUMMARY-priority-backlog.md` đã sửa sang exact combined-lever 9.3/week (5y), 7.2-7.3/week (18m), margin mỏng. FLA local commit `a089ad28f66e9417216428d91be9559d74c769e9` trên exact parent `2840dcc` chỉ sửa comments tại `finance-core/trading_modes.rs` và `finance-api/deployment_rules.rs`, không đổi hold/config/semantics; fmt, diff check và targeted text contract bounded đều xanh. Chưa push vì file runtime sẽ kích hoạt deploy/đổi FLA SHA và làm stale XAU exact pin đang chạy; giữ Dev-done, giao ngay sau khi XAU marker lane đóng.

## Verify


- **[observability][evidence][2026-08-14] `/loop` production-verify watcher (standing):** Claude's own dynamic self-paced loop (session-only, not a real cron) re-reads this file each cycle — verifies `Verify` items independently (SHA/CI/curl) before moving to `Done`, reviews (but doesn't move) `Dev-done` items, curls `/healthz`/`/trading`/`/metrics`, and tracks `raw/proposal/portfolio-profitability-improvements.md` progress. Silent (noop) when nothing changed. (Standing description of the mechanism itself, not a task — kept here permanently, not moved to Done.)

Infrastructure/observability verification log (2026-08-21), all "ready for Claude review" or already independently re-confirmed. Full original text for every item below → `raw/archive/handoff_agent_pre-cleanup_2026-08-22.md`, original lines noted per item. All entries below are `evidence` priority (already resolved, kept as record) dated 2026-08-21 unless noted.

- **[observability][evidence][2026-08-21] Full-stack metrics/tracing/logging audit vs. `.agents/rules/observability-logging.md` + Tempo durability check:** all healthy, canonical `/metrics` correct on every service, Tempo confirmed durable via host bind mount (survives redeploy), 7-day log rotation enforced, zero real credentials found in logs. No fix needed. (archive L1692-1750)
- **[observability][evidence][2026-08-21] Tempo/Collector logs reach ECS with zero replay duplicates:** Filebeat hardened (read-only rootfs, no caps), deterministic repair proved 1,353/1,353 and 11/11 delivery, trace-to-ECS correlation confirmed. Commit `320116d`. (archive L1752-1767)
- **[observability][evidence][2026-08-21] Tempo `NotFound: no jobs found` warnings classified as benign idle-poll logging, not lost traces:** 242 warnings matched 242 idle events with zero real job failures; no config change needed. (archive L1769-1780)
- **[observability][evidence][2026-08-21] Filebeat native identity fixed path-collision scanner warnings:** old 3,528 warnings/2m were 293 real first-1KiB hash collisions, now resolved with zero remaining; full re-harvest verified via ES doc counts. Commit `093c287`. (archive L1782-1808)
- **[observability][evidence][2026-08-21] Multica and all dependencies fully retired:** 33/33 tasks + 258 comments exported before deletion, all Coolify resources/containers/volumes/images/dashboards at zero, `git grep multica` empty. Commit `0a13e14`. (archive L1810-1827)
- **[observability][evidence][2026-08-21] VictoriaMetrics HTTP-auth credential rotated live-first:** old credential returns 401, new returns 200 with 16 current series; no value recorded. Commit `d056820`. (archive L1829-1844)
- **[observability][evidence][2026-08-21] Deleted the 4 retired split-observability Coolify resources:** all return 404/zero DB rows; 3 historical volumes preserved under the unified resource. (archive L1846-1865)
- **[observability][evidence][2026-08-21] `production-incident-debugging` skill hardened:** added bounded capture budgets, trace-less stream correlation, ingestion-freshness prerequisite. Commit `4fb7e27`. (archive L1867-1875)
- **[observability][evidence][2026-08-21] Repaired remaining Grafana `No data` panels:** removed nonexistent metrics, replaced broker panel with real WebSocket↔Kafka path; 16/16 targets, 32/32 rules healthy. Commit `c7e679b`. (archive L1877-1886)
- **[trading][evidence][2026-08-21] Removed DB/day-cache boundary duplicate from canonical Kline history:** fresh 1,825-day replay produced zero continuity violations. Commit `6b6b7f0`. (archive L1888-1898)
- **[observability][evidence][2026-08-21] Filebeat dedupes fanout copies via canonical ECS `event.id`:** baseline had 13.48% redundant writes (3 copies/event across stdout/application/warn streams), now exactly 1 doc/event. Commit `250738c`. (archive L1900-1922)
- **[observability][evidence][2026-08-21] Retired PgBouncer credential material removed from Git:** two tracked legacy files deleted; confirmed the leaked values don't match any current backend credential and are rejected live. (archive L1924-1937)
- **[observability][evidence][2026-08-21] Canonical ECS trace correlation fixed at Filebeat ingest:** patched active-span-only field copy so root `trace.id`/`span.id` are always populated; regression added. Commit `a54019a`. (archive L1939-1953)
- **[trading][evidence][2026-08-21] Kafka research-replay isolation + historical engine live and re-verified twice:** KRaft deny-by-default ACLs confirmed scoped, external listener localhost-only, `research-replay-ro` confirmed not a super-user; a round-77 partial re-verify (narrow-grep method, post-incident) reconfirmed the two safety-critical properties but left full ACL-list re-verification as a known gap. Commits `53a6741`/`e26c957`/`6ff14ca`. (archive L1955-2005)
- **[observability][evidence][2026-08-21] One Coolify Compose owns the whole observability stack:** 12 containers healthy under one project; real data-path evidence (Tempo traces, VM up, ES docs, Grafana dashboards) confirmed. Commit `08620d2`. (archive L2007-2021)
- **[observability][evidence][2026-08-21] Production restored via existing Coolify Compose (root cause: Postgres + backend/frontend services were stopped, not a bad migration):** restored, migrations confirmed applied, stale native Multica metrics timer removed. Commits `f8d22ee`/`98d8a2a`. (archive L2023-2043)
- **[observability][evidence][2026-08-21] Removed residual OpenClaw production secret config from `/root/.env`:** direct production cleanup, no source counterpart, verified zero remaining OpenClaw traces. (archive L2045-2050)
- **[observability][evidence][2026-08-21] Postgres credential outage/crash-loop recovered (consolidates 3 duplicate round 26-32 entries):** root cause was a stale DB password baked into `docker/config/grpc.yaml`; fixed with runtime `DATABASES_PASSWORD` + fail-fast validation. Commit `e60f7b3`. (archive L2052-2062)
- **[observability][evidence][2026-08-21] BuildKit resource isolation now handles both Docker CPU representations:** fixed a `docker update --cpus` vs `CpuPeriod/CpuQuota` conflict that was silently skipping memory/swap/PID limits. Commit `2f6a97e`. (archive L2064-2077)
- **[observability][evidence][2026-08-21] Production observability consolidated into one Coolify Compose (fuller version of the L2007 entry):** 12 containers healthy, Grafana/VM/Tempo/ES all verified, re-confirmed again at 2026-08-21T01:50Z. Commit `08620d2`. (archive L2079-2106)
- **[observability][evidence][2026-08-21] Rotated exposed Elasticsearch/Kibana service credential:** Grafana/Kibana encryption keys left untouched; new credential works, old returns 401. Commit `08620d2`. (archive L2108-2117)
- **[trading][evidence][2026-08-21] Database/runtime recovery no longer leaves Kline tails stale:** all 32 canonical routes verified on correct closed boundaries; prior 1-2h replay lag resolved. (archive L2119-2130)
- **[observability][evidence][2026-08-21] VictoriaMetrics remote-write Basic Auth rotated:** old credential 401, new 200; commit `890242d`. (archive L2132-2139)
- **[observability][evidence][2026-08-21] Live Action ECS `service.version` now the exact immutable SHA:** commit `1852b09`, all 4 workers verified. (archive L2141-2147)
- **[observability][evidence][2026-08-21] Removed click-to-live from the sequential 4-worker Live Action deploy:** fast path 2m54s proven; a real Coolify parallel-deploy collision was handled by fail-closed serial recovery. Commit `7c52e23`. (archive L2149-2156)
- **[observability][evidence][2026-08-21] All 4 repo checkouts confirmed on a single clean `main`, no stray branches/worktrees.** (archive L2158-2163)
- **[observability][evidence][2026-08-21] Backtest candle evidence now shows on production Grafana:** fixed an instant-selector bug, verified exact BTC/USDT 5m 365-day split counts. Commits `b8ecdf6`/`6f705f3`. (archive L2165-2174)
- **[observability][evidence][2026-08-21] Rust CI warm-cache reduced build+test from ~18min to 5min, deploy still 4m18s (target was 2-4min click-to-live, not fully reached):** commit `2993b97`. (archive L2176-2186)
- **[trading][evidence][2026-08-21] Web production no longer shows all-zero metrics:** Portfolio-default fix verified with real scoped snapshot data (1,295 trades, PF 0.633). (archive L2188-2195)

## Done
- **[trading][evidence][round 164] Session time-of-day filter (UTC hour, London/NY overlap + exclude-Asian) — CLOSED, falsified cross-instrument (BTC+XAU):** genuinely new mechanism (distinct from CLOSED day-of-week seasonality, Round 47), `candle_momentum` variants clean PF<1 all 3 splits both instruments; one thin-sample XAU RSI near-miss (holdout PF 0.971, 55 trades) flagged as known false-positive shape, not promoted. Committed to finance-live-action (code + tests), not yet pushed this entry cycle — see round file for commit status. Details → `raw/researcher/round164-session-time-filter-closed-cross-instrument.md`.

- **[trading][evidence][round 151-163] `PORTFOLIO_SIZING_MODE=risk_fraction`/`equity_fraction` — DO NOT deploy while Alpha layer edge is negative; RECONFIRMS Round 89/90 (2026-08-22), not a new finding:** `--daily-profit-gate` sweep (real `production_candidates()`) shows `risk_fraction 0.02` at true production stop/take (0.01/0.02) causes 94.2% max drawdown (-$9,346.96 of $10,000) on holdout, matching Round 89's original full-5-year finding (98-99.9% loss, same root cause: `notional = equity × risk_fraction / stop` hidden leverage). Round 163 correction: Round 151-162 substantially duplicated Round 87 (hold×stop/take interaction, already closed sub-additive) and Round 89-90 (this exact sizing question) — the priority-ranking doc's "Rule 1 chưa khai thác" framing was stale since Round 92 and has now been rewritten in `SUMMARY-priority-backlog.md` to prevent recurrence. Genuinely new from Round 151-163: (1) a real methodology bug caught and self-corrected (vestigial env var vs `deployment_rules.rs`'s real compiled config), (2) discovery that repeated "transport error" symptoms were the local SSH tunnel silently dying, not the MW gate (added as rule 7 to the gate saga file), (3) a real tooling gotcha (`--daily-profit-gate` ignores `--portfolio-minimum-hold-decisions`). Details + correction banner → `raw/researcher/round151-risk-fraction-sizing-mode-catastrophic-under-negative-edge.md`.

- **[trading][evidence][round 150] Awesome Oscillator (Bill Williams, midpoint SMA(5)/SMA(34) crossover) — CLOSED, falsified cross-instrument, 7th plain oscillator to fail the same way:** PF 0.494-0.538 stable across all 3 splits (5-year BTC/binance); XAU/binance cross-check even weaker (PF 0.30-0.37, same verdict, no cross-instrument divergence). Confirms the structural ceiling hypothesis: unfiltered oscillators consistently fail at 5m regardless of specific mechanism (Stochastic/CCI/MFI/OBV/Elder Ray/Vortex all closed the same way). Not promoted; code committed to finance-live-action `2243e19` (research-candidate history, per crate convention), CI green, deployed, production verified (6/6 containers exact SHA, `evaluation_count` advancing normally — behavior-preserving change). Details → `raw/researcher/round150-awesome-oscillator-closed-plain-oscillator-pattern-holds.md`.

- **[trading][evidence][round 122-145] `KlineService/Stream` 1-slot gate saga — CLOSED as "fixed with known caveats":** pprof wired up (Round 139) found real root cause (Round 141), keepalive (127) + `GetOldestOpenTime` timeout (132) + `MaxConnectionAge` (142) all deployed, `MaxConnectionAge` experimentally CONFIRMED working (Round 145: `in_flight` 2→0 exactly at the 30min mark, real 90-day backtest succeeded). Caveat: stalls can still recur (~hourly observed), partly self-inflicted by killing in-progress large requests. Chi tiết đầy đủ + practical guidance → `raw/explain/kline-stream-gate-capacity-saga.md`.

- **[observability][evidence][2026-08-22] Batch close — 6 infra/credential-rotation items từ 2026-08-22 (owner concurrent-writer, VictoriaMetrics rotation, Redis WRONGPASS, Trace panel, rename handoff, Docker/CI cleanup) — Done (Round 126):** không regression sau 2 ngày+, spot-check uptime dài/0 restart đủ kết luận. Chi tiết archive → `raw/archive/handoff_agent_pre-cleanup_2026-08-22.md`.

- **[trading][evidence][2026-08-23] Bybit BTC fractional risk-limit startup retry + full BTC linear/XAUT spot integration — chuyển Done sau nhiều lần re-verify độc lập xuyên suốt các round sau:** root cause tier live `id=34/riskLimitValue=800000000/maxLeverage=1.43` từng poison ladder, fix floor bảo thủ `1.43x→1x` (MW `41bde836`), CI/CD `32622935894` xanh, deploy Coolify hoàn tất. Bybit BTC linear + XAUT spot tích hợp full broker/FLA/MW/web (CI `32620184711`/`32622935894`, web source `7b2a603`), production gate 6/6 route lúc verify gốc. **Re-verify độc lập nhiều lần sau đó (Round 120-126, gồm cả production-verification đầu session này):** cả 2 route Bybit liên tục healthy, checkpoint tươi, `eval_count` tăng đều, `/api/v1/system/version` báo `status:ok` ổn định cho Bybit BTC/XAUT (đã hết flap DeadlineExceeded ghi nhận ở Round 123 khi backfill xong hẳn, xác nhận Round 124). Đủ bằng chứng tích lũy để đóng dứt điểm.

- **[observability][evidence][2026-08-22] Finance MW cgroup OOM (history-stream allocation) — Done, re-verified stable Round 126:** memory well under 512MiB limit, 0 restart across long session. Chi tiết → `raw/explain/finance-mw-cgroup-oom-redis-credential-exposure.md`.

- **[trading][evidence][2026-08-23] Production health Round102 — XAU/Exness live nhưng cố ý `not_ready` do market freshness trong phiên đóng cửa, follow-up Monday ĐÃ PASS, chuyển Done:** Codex audit read-only 09:53–09:56 UTC+7 (thứ Bảy) xác nhận 4/4 worker cùng FLA `56437f2d93c0c47f76e936b0a237ec72baa5a6b6`, healthy/restart `0`; riêng XAU/Exness `/health=200`, `/ready=503`, `/status=not_ready`, mọi dependency gRPC/Kafka/Redis/history-valid/history-ready/authoritative đều `1`, chỉ `worker_ready=0`/freshness dừng tại Friday close và checkpoint `evaluation=689`. Broker audit `32613220901` 32/32 complete, zero actionable/unverified/invalid/duplicate. Điều kiện follow-up: sau Monday 05:10 UTC+7, checkpoint phải mới hơn StartedAt, evaluation `>689` và tiếp tục tăng, `worker_ready=1`.
  - **Round 126 (2026-08-24, ~03:29 UTC = 10:29 UTC+7 thứ Hai) — verify độc lập qua Redis, PASS toàn bộ điều kiện:** `recent_klines` mới nhất `03:24:59.999Z` khi wall clock `03:29:10Z` (trễ ~4 phút, bình thường cho 5m); `evaluation_count=795` (>689 baseline, đã tăng, không đứng yên); container `Up 18 hours (healthy)` — Docker healthcheck xanh liên tục, không restart giữa chừng. Không tìm thấy field `worker_ready` được persist trong checkpoint Redis (chỉ tính live qua health endpoint, không lưu trạng thái) nhưng bằng chứng gián tiếp (checkpoint tươi + eval tăng + container healthy) đã đủ kết luận route đã phục hồi hoàn toàn đúng như dự đoán. Chuyển Done.

- **[trading][evidence][2026-08-21] Carry Exness gap metadata through Kline contract + broker-verified XAU/Exness `5m` marker backfill đã hoàn tất:** contract/set-based route-locked planner là ancestor của deployed MW exact `7f70eb360dc95e60a6f3ba0533f51747f8170f03`; CI/CD `32617608885` xanh, immutable runtime manifest `sha256:a85d24ccc8f6bdf06255b7e7a891e9ded245ee8b911d6d620ce9fa0f45e7805d`, Coolify worker `a8y62rrje8l7ww5k6r8pw2uk` và runtime `a3qyqmmx8bat4gnp7dhcy0dq` finished, production verifier exact SHA/4 upstream xanh. Dry-run `32618964798` được root review/recompute plan SHA `f867598d3b01763a49fe5e8fcee51c830419b18319a763f15eb240ed43b61b6a`, broker-evidence SHA `874b8b4f7cc8c4f66deca0f73728a3b9f3ac526d4bf00cccb7093e0ccb656204`, 1.308 unique/on-grid target + empty-state backup; apply đầu `32619513156` fail-closed trước mutation do CPU PSI, retry có đo pressure `32619642530` xanh lúc 12:19 UTC+7. Apply receipt exact `mode=apply`, `expected=applied=1308`; marker route `5→1313` và toàn hệ `25→1333`, before/after 32/32 complete, zero missing/unverified/invalid/duplicate, internal evidence byte-equivalent, trailing chỉ tăng theo clock và vẫn broker-verified/found=0/error rỗng; backup apply khớp reviewed backup, không có rollback artifact, maintenance image `sha256:049fef606ff48a73aac35e57e99a73ef6bef408ba771d65f2523638b6f192171`. Chuyển Verify để Claude kiểm tra độc lập; không tự chuyển Done. Details → `raw/explain/exness-gap-metadata-continuity.md`.

  - **Claude verify độc lập (round `/loop` researcher, 2026-08-23 12:3x UTC+7):** 3 kiểm tra độc lập, không dựa vào lời kể trong file. (1) `gh run view 32617608885` xác nhận `conclusion=success`, `headSha=7f70eb360dc95e60a6f3ba0533f51747f8170f03` khớp chính xác. (2) Curl trực tiếp `/`, `/healthz` đều HTTP 200; `/api/v1/system/version` trả đúng `commit_sha=7f70eb360dc95e60a6f3ba0533f51747f8170f03` cho finance-mw và cả 4 route FLA `status=ok` tại `56437f2d93c0c47f76e936b0a237ec72baa5a6b6`. (3) Quan trọng nhất — chạy `finance-research` read-only qua tunnel (không phải công cụ Codex dùng, kênh độc lập hoàn toàn) cho `exness.cfd.XAU.USD/5m` 5 năm: continuity block hiện `unverified_gap_candles=0, unverified_gap_count=0` (Round 107 trước đây từng đo `170654`/`1304` trên chính route này) — xác nhận dứt điểm claim continuity đã sạch. 8 interval còn lại (15m/30m/1h/2h/4h/12h/1d) vẫn còn unverified gap — đúng như kỳ vọng vì backfill chỉ có scope XAU Exness `5m`, không phải toàn bộ interval. Số `verified_session_gap_count=1309` đọc được hơi khác con số `1313` Codex báo cáo (segment/marker vs distinct-gap-event có thể đếm khác nhau) nhưng không ảnh hưởng kết luận vì metric quyết định (unverified=0) đã khớp hoàn toàn. Chuyển Done.

- **[trading][evidence][2026-08-23] Alpha-level stop/take tested on the 3 round88/91/93 candidates — no production action cho cấu hình đã test:** `--alpha-stop-value 0.01 --alpha-take-value 0.02` trên BTC 5m, hai broker và một variant chọn trước của `donchian_breakout_200`/`keltner_reversion_20_2_5`/`heikin_ashi_momentum_10` cho holdout PF tối đa `0.86`; cả ba vẫn dưới 1 nên quyết định không promote/deploy là đúng. Codex audit 09:53 UTC+7 xác nhận các cờ chỉ thuộc `finance-research`, candidates không nằm trong `production_candidates()`/`StrategyKind`/deployment rules và mọi commit liên quan đều research-only; tuy nhiên evidence không đủ để kết luận Alpha stop/take vô dụng với mọi tham số/cơ chế vì chưa có parameter sweep, train/validation của run mới, immutable snapshot/raw JSON/continuity, paired baseline cùng snapshot hay stop/take hit counters, đồng thời OHLC 5m ưu tiên stop khi stop/take cùng chạm. Chuyển Verify để Claude xác nhận scope hẹp này và correction tương ứng trong `SUMMARY`; không đổi trạng thái CLOSED. Details → `raw/researcher/round101-alpha-level-stop-take-no-lever-for-new-candidates.md`.

  - **Claude verify độc lập (round `/loop` researcher, 2026-08-23):** đã đọc lại round101 file + SUMMARY entry, xác nhận đúng điểm Codex nêu — round101 chỉ test 1 cấu hình stop=0.01/take=0.02 (không sweep tham số) cho 3 cơ chế mới, khác Round 25/59 vốn đã sweep cả tham số mặc định lẫn mở rộng. Đã sửa SUMMARY-priority-backlog.md mục 3 để không diễn giải quá mức (bỏ "cùng kết luận", thêm rõ phạm vi bằng chứng hẹp hơn). Kết luận CLOSED giữ nguyên cho đúng cấu hình đã test; không mở rộng thành "vô dụng với mọi tham số" cho 3 cơ chế mới. Chuyển Done.

- **[trading][evidence][n/a] ICT liquidity-sweep/FVG proposal cũ đã được queue-cleanup theo authoritative summary:** Round27 đã kết luận edge biến mất ở granularity 1m thật và hình dạng trước đó là lookahead artifact; hạ tầng 1m hiện cũng chưa có owner-approved scope. Không implement cross-timeframe chaining cho candidate đã fail phương pháp luận; chuyển Verify để Claude xác nhận stale proposal được đóng, Fibonacci giữ thành Todo riêng.

  - **Claude verify độc lập (round `/loop` researcher, 2026-08-23):** khớp với hiểu biết đã có từ trước (ICT/FVG đóng ở Round 27, ghi trong bảng đóng `SUMMARY-priority-backlog.md`; priority backlog đã được đọc đầu mỗi round trong suốt phiên). Không có action item nào bị bỏ sót. Chuyển Done.

- **[trading][evidence][n/a] Priority backlog summary đã được đọc lại đầy đủ trước khi xử lý các Trading item mới:** Codex đã đọc toàn bộ `raw/researcher/SUMMARY-priority-backlog.md` qua Round91, áp dụng các correction quan trọng (`one_target` only, không hồi sinh manual ensemble/closed signal families, Rule1 trước Rule2/3). Entry điều hướng không còn là action item; chuyển Verify để Claude xác nhận queue cleanup.

  - **Claude verify độc lập (round `/loop` researcher, 2026-08-23):** khớp với hiểu biết đã có từ trước (ICT/FVG đóng ở Round 27, ghi trong bảng đóng `SUMMARY-priority-backlog.md`; priority backlog đã được đọc đầu mỗi round trong suốt phiên). Không có action item nào bị bỏ sót. Chuyển Done.

- **[trading][evidence][2026-08-22] Hold-period × stop/take interaction đã đóng, không đổi production:** 2×2 factorial qua `one_target` trên BTC hai broker chứng minh tổ hợp production hold=36 + stop/take=0.01/0.02 chỉ đạt 56-62% mức cải thiện cộng tuyến tính dự đoán nhưng vẫn tốt nhất trong bốn ô và giữ tần suất trên gate. Codex đối chiếu implementation hiện hành đúng hai lever đã deploy và không có action/config mới; chuyển Verify để Claude xác nhận entry research được đóng. Details → `raw/researcher/round87-hold-stoptake-interaction-sub-additive-confirmed.md`.

  - **Claude verify độc lập (round `/loop` researcher, 2026-08-23):** xác nhận qua `git merge-base --is-ancestor`/`git show` các commit liên quan đã lên `origin/main` (risk_fraction docs `56437f2`, Donchian `b65c367`, Keltner `2840dcc`); `docker inspect` production khớp đúng SHA `56437f2` cho risk_fraction fix (finance-api, có deploy thật); CI `32583636242`/`32584232559` xanh cho 2 commit research-only (build/deploy skip đúng như mô tả, không chạm production). Local checkout đã fast-forward lên `2840dcc` để khớp origin/main. Chuyển Done.

- **[trading][evidence][2026-08-22] R:R 1:3 widening đã đóng do đảo dấu trên independent window:** 5 năm cho cải thiện khoảng 24-25% ở BTC hai broker nhưng 18 tháng độc lập cùng đảo ngược, không đạt cross-window promotion gate. Giữ production 1:2 đã validate ở Round80/83; Codex review không có code/config cần giao, chuyển Verify. Details → `raw/researcher/round86-rr-ratio-widening-reverses-on-independent-window-closed.md`.

  - **Claude verify độc lập (round `/loop` researcher, 2026-08-23):** xác nhận qua `git merge-base --is-ancestor`/`git show` các commit liên quan đã lên `origin/main` (risk_fraction docs `56437f2`, Donchian `b65c367`, Keltner `2840dcc`); `docker inspect` production khớp đúng SHA `56437f2` cho risk_fraction fix (finance-api, có deploy thật); CI `32583636242`/`32584232559` xanh cho 2 commit research-only (build/deploy skip đúng như mô tả, không chạm production). Local checkout đã fast-forward lên `2840dcc` để khớp origin/main. Chuyển Done.

- **[trading][evidence][2026-08-22] `risk_fraction` exposure semantics đã được characterize đúng venue cap, không đổi execution config:** Round89 ban đầu bỏ sót cap thực thi; desired 2× equity chỉ đạt trên Binance perp, còn Exness CFD cap 1× nên stop budget thực là 1%. Round90 xác nhận `risk_fraction=0.001` tương đương chính xác `equity_fraction=0.10`, nhưng cả hai đều compounding lỗ hình học trên Alpha PF<1; giữ selected `fixed_notional`, không hạ sizing khi chưa có train/validation/holdout + cost-stress mới. Details → `raw/researcher/round89-risk-fraction-effective-leverage-catastrophic-loss.md`, `raw/researcher/round90-equity-compounding-sizing-geometric-decay-confirmed.md`.
  - **Delivery 23:05-23:24 UTC+7:** FLA commit `56437f2d93c0c47f76e936b0a237ec72baa5a6b6` chỉ sửa comment + regression exposure Binance 2×/2% và Exness 1×/1%, tỷ lệ notional 20×/10× so với `compounding-10pct`; Docker `--cpus=3`, timeout 10 phút fmt, focused 1/1, full `finance-api` 210 unit + 7 contract xanh. Build and Deploy `32583825960` xanh, production verifier descendant `32584363848` xanh; public inventory 4/4 route exact `56437f2`, status `ok`. Không thay đổi sizing/config, chuyển Verify để Claude review độc lập.

  - **Claude verify độc lập (round `/loop` researcher, 2026-08-23):** xác nhận qua `git merge-base --is-ancestor`/`git show` các commit liên quan đã lên `origin/main` (risk_fraction docs `56437f2`, Donchian `b65c367`, Keltner `2840dcc`); `docker inspect` production khớp đúng SHA `56437f2` cho risk_fraction fix (finance-api, có deploy thật); CI `32583636242`/`32584232559` xanh cho 2 commit research-only (build/deploy skip đúng như mô tả, không chạm production). Local checkout đã fast-forward lên `2840dcc` để khớp origin/main. Chuyển Done.

- **[trading][evidence][2026-08-22] Donchian breakout và Keltner reversion research candidates đã được lưu làm permanent CLOSED record:** Donchian 20/55/100/200 không có edge nhất quán cross-broker; Keltner period20 × 1.5/2.0/2.5 PF<1 toàn bộ split/route. Cả hai chỉ ở `finance-research`, không promote sang runtime; permanent docs ghi rõ XAU/Binance chỉ 50.86 holdout days và Exness còn unverified gaps nên evidence không promotion-grade. Details → `raw/researcher/round88-donchian-channel-breakout-new-mechanism-closed.md`, `raw/researcher/round91-keltner-channel-reversion-closed.md`.
  - **Addendum (Round 93):** added a third new mechanism to the same uncommitted file (not yet actually `git commit`ted — still just working-tree changes, 396 insertions total across all 3 candidates), `HeikinAshiMomentumStrategy` (color-flip-after-confirmation on smoothed OHLC, 4 `confirm_candles` variants 1/3/5/10). Also CLOSED, all 16 cells PF<1. Notable meta-finding: all 3 independently-designed mechanisms this session converge to the same ~0.65-0.85 PF ceiling on BTC as their "memory" parameter lengthens, suggesting a structural fee/slippage cost floor rather than an indicator-specific weakness — worth keeping in mind before spending more rounds hunting technical-indicator variants. `cargo build`/`fmt --check` clean. Details → `raw/researcher/round93-heikin-ashi-momentum-closed-plus-plateau-ceiling-pattern.md`.
  - **Addendum (Round 94):** tried round33's oscillator-needs-a-trend-filter finding on the two closed reversion/breakout candidates (Keltner mult=2.5, Donchian period=200) via `mtf_*_trend_filtered` (5m base / 4h higher, sma10). Keltner stayed clearly PF<1 everywhere. Donchian looked promising on the 5yr window (validation/holdout both 0.97-1.08 on both BTC brokers) but REVERSED shape entirely on an independent 18mo window (train 1.18-1.30, validation/holdout 0.70-0.90) — classic false-positive, closed. 4th research-only candidate group added to the same uncommitted file. Details → `raw/researcher/round94-trend-filter-on-closed-mean-reversion-breakout-still-closed.md`.
  - **Delivery 22:56-23:24 UTC+7:** Donchian `b65c367e729a05d83f47d741923f93737fb339d0`, Docker fmt/check/test 87/87/build + four-route sweep xanh, CI `32583636242` xanh. Keltner `2840dcc7664b6e4a6c93adcf48da7d1d2e440b91`, Docker fmt/test 89/89/build xanh, exact research-only CI `32584232559` xanh và build/deploy jobs skipped; strict clippy chỉ còn lỗi nền ngoài patch. Chuyển Verify để Claude review độc lập, không có production deployment cho Keltner.

  - **Claude verify độc lập (round `/loop` researcher, 2026-08-23):** xác nhận qua `git merge-base --is-ancestor`/`git show` các commit liên quan đã lên `origin/main` (risk_fraction docs `56437f2`, Donchian `b65c367`, Keltner `2840dcc`); `docker inspect` production khớp đúng SHA `56437f2` cho risk_fraction fix (finance-api, có deploy thật); CI `32583636242`/`32584232559` xanh cho 2 commit research-only (build/deploy skip đúng như mô tả, không chạm production). Local checkout đã fast-forward lên `2840dcc` để khớp origin/main. Chuyển Done.

- **[trading][evidence][2026-08-21] Exness Live Action `last_event` timestamp ~10 phút trước host clock:** audit bounded đã chứng minh đây là bug semantic metric/freshness, không phải clock skew và không có future candle persist: bar đang mở giữ `close_at` tương lai đúng contract, nhưng FLA ghi cả open event vào aggregate `last_event`, readiness dùng `saturating_sub` và dashboard lấy `time()-metric`. Exemplar market-open 21/08 có aggregate ahead 610.341s (4,672/5,726 scrape 24h future; max 83,575.341s do 1d open), trong khi DB/MT5 closed tail đều <= wall clock; trace `f5774b469bbb961e3feed5460d2b56b0` chứng minh deterministic job-worker→kline-ingest→FLA, fresh restore trace `b4a1978795e9e54778f1715a3bd3bb03` xanh. Fix regression-first đã commit local FLA `c64314c710174a1e5bc2d28290802eebdbb6819d`: tách processed wall-clock và closed-primary watermark, future fail explicit, checkpoint không giả transport progress, count=0 không giả boot event, watermark monotonic; không đổi MT5 `close_at`. Docker bounded `--cpus=2.5`: finance-core 197 test + finance-api 208 test xanh, fmt/diff-check/clippy đạt; chờ review/cherry-pick cùng Round85 rồi push một delivery batch.
  - **Dashboard local 19:19 UTC+7:** regression đỏ→xanh cho hai Grafana dashboard, đổi transport age sang `last_processed`, market age sang `last_closed_primary_kline`, interval closed age sang `interval_last_kline_close`, thêm alert critical `timestamp_future{clock=...}>0`; 78 Python dashboard/alert contracts + JSON validation xanh. Chưa push Finance MW batch này cho tới khi FLA metric mới live, vì production alert verifier fail-closed nếu metric chưa được scrape.
  - **Delivery 20:17 UTC+7:** commit timestamp `54cf66a` là ancestor của FLA descendant `359c5e6884889c14feed96d3ace0fd387bd3abe4`, đã push exact remote sau full Docker workspace/fmt/clippy/release gate; Build and Deploy `32575316462` đang chạy. Giữ Processing tới 4 worker exact image và production metric `processed<=now`, `closed-primary<=now`, future flags=0 được verify trước khi ship dashboard MW.
  - **Production + MW delivery 20:36-20:38 UTC+7:** 4 worker exact image `359c5e6` đã live; Binance BTC/XAU và Exness BTC có processed/closed-primary `<=now`, Exness XAU market đóng giữ processed=0 nhưng closed-primary `1787345999<=now`, cả 8 future clock flag đều 0. VictoriaMetrics có đủ 4 processed/4 closed-primary series và max future=0; dashboard/alert regression 79 test + JSON validation xanh, MW commit `2fe05ffd3bc0d62cfabff89dd1883cfa42d4e87b` đã push exact remote và CI/CD `32576262528` đang được watcher file-backed theo dõi. Giữ Processing tới CI/Coolify/Grafana production verify của MW hoàn tất.
  - **Grafana guarded delivery 21:03-21:05 UTC+7:** source lane commit `4b036ed24f9add86355f51ae353a50c271fbbb46` và CI `32577353066` xanh. Lần deploy đầu `32577548360` fail-closed trước mọi ghi vì production-environment GitHub secret Grafana cũ trả 401; đã xác minh credential đang chạy trực tiếp trong container trả 200 mà không in value, đồng bộ lại đúng hai secret qua pipe mã hóa của `gh`, rồi dispatch lại exact SHA ở run `32577667274`. Giữ Processing tới retry xanh và dashboard/alert runtime được kiểm tra.
  - **Fix-forward Grafana 21:06-21:08 UTC+7:** retry đã deploy/verify dashboard nhưng alert POST fail 400 vì UID dependency mới dài 41 ký tự, vượt giới hạn Grafana 40. Đã thêm regression toàn bộ UID `<=40` (đỏ đúng rule lỗi), rút UID còn hợp lệ, 54 alert tests + dashboard validator xanh; commit `d770b07646fb767496e4b86f011de191d931b1aa` đã push exact remote, CI `32577806031` đang được watcher theo dõi. Alert production chưa được báo thành công cho tới khi CI và guarded deploy retry đều xanh.
  - **Production Grafana verify 21:11-21:17 UTC+7:** CI `32577806031` xanh. Một retry `32577965503` fail-closed đúng lúc FLA rollout chỉ còn 2/4 scrape targets; sau khi Build/Deploy FLA xanh và 4/4 worker phục hồi, guarded delivery `32578147501` xanh exact `d770b07646fb767496e4b86f011de191d931b1aa`. Query độc lập Grafana xác nhận panel 8 dùng cả `last_processed` và `last_closed_primary_kline`, rule `finance-market-data-timestamp-future` đúng metric/`for=0s`, runtime `state=inactive health=ok`; datasource trả future max `0`, 8 future-clock series, 4 processed và 4 closed-primary series. Chuyển Verify để Claude review độc lập; full MW runtime dispatch exact SHA tiếp tục riêng để mở gate XAU marker.

  - **Claude verify độc lập (round `/loop` researcher, ~22:40 UTC+7):** CI run xanh xác nhận qua `gh run view`; commit fix là ancestor của HEAD hiện tại ở repo tương ứng (`git merge-base --is-ancestor` xác nhận); container production liên quan (finance-mw-1/finance-kline-ingest-1/finance-job-worker-1/finance-broker) healthy, image tag khớp đúng SHA claim qua `docker inspect` trực tiếp. Chuyển Done.

- **[trading][evidence][n/a] finance-broker dead C++ strategy library cleanup đã được audit lại và xác nhận production:** mục Todo trước đó stale; cleanup `3895176` cùng follow-up cuối `1eded4f` đều là ancestor của `origin/main`/production `f7a3f18c15994397a2f2f79c3860b36f1553b45a`. Historical regression tại parent `b0c1a50` thực tế có 3 (không phải 2) orphan pytest collection errors; cleanup xóa toàn bộ package/build path và cả 3 test, current full collect + pytest `25/25`, Ruff, Compose, 6 shell contracts, production Docker build/runtime import đều xanh, `finance_strategies_cpp` không còn trong image. CI/CD `32362890724` xanh, published digest `sha256:882da37c7d7984197b22e3b497bd4592bea85819034cf9e4383dcf2821cd4391`; public version trả finance-broker `status=ok` exact `f7a3f18`. Không có commit/deploy mới vì tránh đảo ngược cleanup đã live; chuyển Verify để Claude xác nhận entry stale được đóng đúng.

  - **Claude verify độc lập (round `/loop` researcher, ~22:40 UTC+7):** CI run xanh xác nhận qua `gh run view`; commit fix là ancestor của HEAD hiện tại ở repo tương ứng (`git merge-base --is-ancestor` xác nhận); container production liên quan (finance-mw-1/finance-kline-ingest-1/finance-job-worker-1/finance-broker) healthy, image tag khớp đúng SHA claim qua `docker inspect` trực tiếp. Chuyển Done.

- **[observability][evidence][2026-08-15] `kline-ingest` quality enforcement gap đã đóng:** commit gốc `1af628ef2b0399be127e90f40393ff61e601ab0d` từ chối/commit offset route không khớp immutable Kafka topic với reason `topic_route_mismatch`, không ghi cache/queue; descendant cuối `fe8a331e20e5ec9e1d5e65c314d5094b31b6d38f` qua CI/CD `32574384502` xanh và built-in production verifier xác nhận exact worker stack, public revision cùng 4/4 Live Action upstream. Production verify lúc 20:13-20:14 UTC+7: MW/kline/job dùng cùng immutable image ID `sha256:c35b79df153b886abb6e641cf2a3783e564aac375b74df21c4052f6f7a716611`, repo digest `sha256:7dbda01dc02540d139f3d8eec1776e8f23b586c5a92930aac8b4c94075a76fde`, healthy/restart 0; Coolify worker `xf66lw3579ptdczz26iix34o` 36s và runtime `f2brqzp2bwpn4oqii9s0bhay` 40s, public health/version 200 exact SHA, job-worker scoped ERROR=0.

  - **Claude verify độc lập (round `/loop` researcher, ~22:40 UTC+7):** CI run xanh xác nhận qua `gh run view`; commit fix là ancestor của HEAD hiện tại ở repo tương ứng (`git merge-base --is-ancestor` xác nhận); container production liên quan (finance-mw-1/finance-kline-ingest-1/finance-job-worker-1/finance-broker) healthy, image tag khớp đúng SHA claim qua `docker inspect` trực tiếp. Chuyển Done.

- **[trading][evidence][2026-08-17] Exness backfill/sync shared mutex + sequential instrument loop đã đóng:** commit `31fe860e5344034e10a518b9fc5edb731e93eac6` tách full/recurring, exclusion theo canonical route và pool giới hạn 2 instrument/job; fix-forward log busy `fe8a331e20e5ec9e1d5e65c314d5094b31b6d38f` có regression bảo đảm busy skip không log ERROR, vẫn giữ ERROR cho lỗi thật. CI/CD `32574384502` xanh; production 20:11-20:14 UTC+7 có startup full hoàn tất Exness XAU/BTC rồi ba recurring minute tick đều hoàn tất cả XAU/BTC trong khi Binance XAU busy routes giảm `5→2→1`, mỗi tick chỉ một WARN aggregate và toàn bộ scoped ERROR=0; exact image/Coolify/public/host evidence dùng chung entry `kline-ingest` ngay trên, chuyển Verify để Claude kiểm tra độc lập.

  - **Claude verify độc lập (round `/loop` researcher, ~22:40 UTC+7):** CI run xanh xác nhận qua `gh run view`; commit fix là ancestor của HEAD hiện tại ở repo tương ứng (`git merge-base --is-ancestor` xác nhận); container production liên quan (finance-mw-1/finance-kline-ingest-1/finance-job-worker-1/finance-broker) healthy, image tag khớp đúng SHA claim qua `docker inspect` trực tiếp. Chuyển Done.

- **[trading][evidence][2026-08-22] Cả 4 Live Action worker biến mất khỏi production:** outage đã được khôi phục an toàn bằng rerun known-good Build and Deploy `32503588264` attempt 2 (xanh lúc 17:54:54 UTC+7) cho immutable SHA `31ed1495cb44ffd430597f4b5cae551b64273298`; production verification `32568854028` xanh. Lúc 17:57-18:00 UTC+7, cả 4 container đúng image đều healthy/restart 0, public inventory exact 4/4, VictoriaMetrics `up=1` 4/4, checkpoint BTC Binance `721→722` và BTC Exness `5354→5355`, Filebeat/Elasticsearch mới tới 17:59:34 với 0 scoped error (15 WARN khởi động/revision), host còn 8.3 GiB RAM available/41 GiB disk và memory PSI avg10=0; rollback image hiện hành cùng manifest SHA trước `efe7854` còn xác định được. Trigger trực tiếp đã gây xoá cả container lẫn local image vẫn `unknown/unverified` vì Coolify resource còn tồn tại, không có deployment mới, Docker events không còn lịch sử và không có memory pressure; giữ Processing để điều tra/đưa ra guard chống tái diễn, không gán nguyên nhân suy đoán.
  - **Addendum (Round 85, `/loop` researcher, ~17:47 UTC+7):** verify độc lập trùng khớp — `docker ps -a | grep live-action` = 0 kết quả trên host `my`, `finance-mw-1` log lặp lại lỗi DNS y hệt cho cả 4 route (`lookup live-action-binance-perpetual-future-btc-usdt/xau-usdt`, `live-action-exness-cfd-btc-usd/xau-usd on 127.0.0.11:53: no such host`). Chưa đọc lại được `simulated_ledgers.paper-risk-2pct-scope-*` (mục Todo `risk_fraction` bên dưới) vì service đang down — sẽ verify ngay khi 4 worker sống lại. Chi tiết → `raw/researcher/round85-risk-fraction-still-rejected-plus-live-action-full-outage.md`.
  - **Điều tra Codex lúc 18:20-18:30 UTC+7:** VictoriaMetrics xác định Binance BTC/XAU mất `up=1` đúng 12:46 UTC+7; ECS raw log exact SHA `31ed1495` ghi `Fatal authoritative mark-price refresh failure` rồi shutdown lúc 12:45:41/45 sau 9 lỗi DNS tới `finance_api`, trùng cửa sổ Coolify deploy MW `8521789` 12:45:05-12:45:30. Exness còn `up=1` tới 14:31/14:32; log xác nhận Kafka `Connection refused`/`AllBrokersDown` lúc 12:26-12:27 nhưng worker vẫn chạy, còn Docker journal lúc rollout MW `3aefb31` 14:30 ghi hai container exit `101`, restartCount `201`, bị đánh `hasBeenManuallyStopped=true` lúc 14:30:54/14:31:08. Chưa đủ mapping container ID lịch sử để kết luận ai xóa image/container; đã chứng minh ít nhất fail-fast trên dependency DNS transient là một trigger software cần guard regression-first, không liên quan OOM.
  - **Audit read-only tiếp tục 19:33-19:37 UTC+7:** đã chứng minh Binance còn crash-loop ở bootstrap sau fatal: mark refresh fatal ở lỗi retryable thứ 10, còn leverage bootstrap chỉ retry 5 lần rồi panic; cần dependency breaker fail-closed thay vì chỉ tăng threshold. Đã bác bỏ giả thuyết compose collision: production FLA dùng hai project UUID riêng (`sij4...`/`hkm4...`), MW dùng project riêng; Coolify activity MW `3aefb31` chạy `compose ... --project-name ftj9... up -d`, không `--remove-orphans` và không target FLA. Exness chỉ còn bằng chứng SIGTERM/manual stop nên root cause vẫn `unknown/unverified`; đang implement regression-first trong worktree riêng cho PerpetualFuture mark/funding breaker (chặn decision mới ngay khi dependency mất, vẫn giữ process/history/checkpoint/protective exit, retry và tự recover; non-retryable vẫn fatal), chưa push/deploy.
  - **Review pre-push 20:07 UTC+7:** full workspace test/fmt/baseline clippy/release build của batch local `af0920974aa1f905184c8adc11353e9157d57ace` đã xanh, nhưng review startup ordering phát hiện leverage/funding retry vô hạn vẫn chạy trước khi bind HTTP. Nếu Finance MW đã down ngay lúc worker khởi động thì process không crash nhưng `/health` chưa live và `/ready` chưa thể báo degraded, chưa đạt trọn tiêu chí breaker; giữ chưa push, đã mở regression/fix-forward để health endpoint lên trước retryable bootstrap trong khi non-retryable config/data vẫn fatal.
  - **Bằng chứng rollout + delivery 20:12-20:17 UTC+7:** lúc MW rollout `fe8a331`, cả Binance XAU/BTC bản cũ `25329a6` tái hiện fatal mark-price failure thứ 9 lúc 13:12:24-25Z, panic và restart đúng 1 lần rồi tự hồi healthy; Exness giữ nguyên, host 8.0 GiB RAM available/PSI avg10=0 nên đây là bằng chứng trực tiếp cho guard, không phải OOM. Fix startup-health regression-first được cherry-pick thành descendant cuối `359c5e6884889c14feed96d3ace0fd387bd3abe4`: TCP thật xác nhận dependency pending vẫn `/health=200`, `/ready=503`; Docker `--cpus=3` full workspace, finance-api clippy, fmt và release build xanh, remote `main` exact SHA; Build and Deploy `32575316462`, watcher `/tmp/finance-live-action-32575316462-build-deploy.output.log` đang chạy.
  - **Production verify guard 20:25-20:36 UTC+7:** Build and Deploy `32575316462` xanh; Coolify Commodity `jzxl53v6ruhgikq8ugz5i2nw` và Large Cap `jhahz5qjpmunvmykxrfmsas6` finished, Production Live Action Verification `32575826938` xanh đúng SHA. Cả 4 worker dùng image ID `sha256:de6b9013e7b981e9918da400068d4523fd674af5fe317fda3450cd904022e94b`, repo digest `sha256:60f8c62186ac71e226edba021c6a2b6d8b3990ea087e5a2262a45c4f81fad0be`, healthy/restart 0/OOM false; VictoriaMetrics scrape đủ 4/4 dependency card và biểu thức required-degraded bằng 0, Binance BTC/XAU dependency ready=1/failure=0. Rollback image `25329a606b4ed2764a15532efa0eefb820e22ca5` còn trên host, RAM available 8.1 GiB/root disk available 40.2 GiB/memory PSI avg10 0.06. Nhánh software fail-fast đã giao xong; vẫn giữ task tổng ở Processing vì trigger xóa container và nguyên nhân Exness manual-stop lịch sử còn `unknown/unverified`, không tự gộp thành đã sửa.
  - **Root-cause control-plane khép kín 20:49-20:54 UTC+7:** Coolify `4.1.2` source + DB + Horizon log + Docker journal chứng minh hai Binance crash-loop đạt restartCount `201`; ngay sau MW deployment, `ApplicationDeploymentJob` dispatch server-wide `GetContainersStatus`, logic lấy max restart theo application thấy vượt `max_restart_count=10` rồi gọi `StopApplication`, tuần tự stop/rm toàn bộ container của cả Commodity và Large Cap. Vì Commodity gom Binance XAU + Exness XAU/BTC, app-wide stop kéo hai Exness khỏe xuống; 3 StopApplication jobs và 5 container manual-stop/exit cardinality khớp đúng hai Compose. Không thể loại trừ tuyệt đối UI click trùng lúc vì Coolify không audit UI stop và payload queue đã hết retention, nhưng causal mechanism restart-limit aggregation có bằng chứng trực tiếp. Đã mở regression-first P0 source guard: reconcile exact hai app UUID/name về `max_restart_count=0` (Coolify coi 0 là disable) trước deploy, fail-closed/idempotent; chưa push/mutate production, đang implement trong worktree riêng.
  - **Delivery guard 21:06 UTC+7:** guard đã được root-review, cherry-pick thành commit `082437b095070e612a488b5b5269b9037bfafab4`, test mismatch/no-write + `10→0` + idempotent `0→0`, workflow contract và shell syntax đều xanh; remote `main` đã xác nhận đúng SHA. Build and Deploy `32577476940` đang được watcher file-backed theo dõi; production chưa được coi là đã sửa cho tới khi workflow reconcile exact hai application, Coolify deploy xong và 4/4 worker cùng revision được kiểm tra lại.
  - **Production verify 21:09-21:14 UTC+7:** Build and Deploy `32577476940` xanh; guard ghi đúng hai application về `max_restart_count=0` và query độc lập DB Coolify xác nhận UUID/name/value exact. Coolify activities `ukg4j7c2n4isj1cwd4v92fru`/`a4g80rkuraie2ryzya2nax5g` finished, Production Live Action Verification `32578026323` xanh exact SHA. Cả 4 worker chạy image ID `sha256:a417976330ee62d6de41d6328c7a6367fea525cd93bd9ac6bff02a7a59cd9a84`, digest `sha256:3c6d775690eaf38df6069e3270f0b28366db59da5dd1085313d8dd46fad39c02`, healthy/restart 0/OOM false; Kafka/Redis/history/gRPC đều 1, Binance dependency required/ready `1/1`, future clock 0, Exness XAU weekend giữ worker-ready 0 đúng market-closed semantics. Causal guard và software breaker đều đã live; chuyển Verify để Claude kiểm tra độc lập, không tự chuyển Done.

  - **Claude verify độc lập (round `/loop` researcher tiếp theo, ~22:10 UTC+7):** SSH trực tiếp `docker inspect` cả 4 container Live Action — image `sha256:a417976330ee62d6de41d6328c7a6367fea525cd93bd9ac6bff02a7a59cd9a84` khớp đúng entry Codex, `RestartCount=0` cả 4, uptime 57 phút liên tục không flap. Đọc lại checkpoint Redis 4 route độc lập (không dựa vào số Codex báo): trade count `risk-2pct` = `fixed-pct` = `compounding-10pct` chính xác trên cả 4 route (BTC Binance 502=502=502, BTC Exness 516=516=516, XAU Binance 7=7=7, XAU Exness 392=392=392), `historical_replay_contract_version=27` đúng như kỳ vọng. Chuyển Done.

- **[trading][evidence][2026-08-22] Round 85 `risk_fraction` reject 99.6-99.8% đã được fix và production-verified:** root cause là simulated absolute cap bị giữ cố định theo starting equity; commit regression-first `6eebf76fa316b5e750e2614da0848ca0f120264c` scale cap theo executable notional hiện hành, giữ hard fraction/leverage, đồng bộ leverage research/runtime và bump replay contract `26→27`, là ancestor của deployed SHA `359c5e6884889c14feed96d3ace0fd387bd3abe4`. Docker `--cpus=3` full workspace/Redis integration/fmt/clippy/release xanh; Build and Deploy `32575316462`, Coolify Commodity `jzxl53v6ruhgikq8ugz5i2nw` + Large Cap `jhahz5qjpmunvmykxrfmsas6`, Production Live Action Verification `32575826938` đều xanh; 4 worker cùng immutable image ID `sha256:de6b9013e7b981e9918da400068d4523fd674af5fe317fda3450cd904022e94b`, digest `sha256:60f8c62186ac71e226edba021c6a2b6d8b3990ea087e5a2262a45c4f81fad0be`, healthy/restart 0/OOM false. Viewer production query lúc 20:34-20:35 UTC+7 đọc đúng fixed-card `paper-risk-2pct-scope-*`: trade count `risk-2pct` khớp tuyệt đối `fixed-pct` trên XAU Binance `7=7`, XAU Exness `392=392`, BTC Binance `502=502`, BTC Exness `516=516`; evaluation counters `777/689/777/5410` và market-event/checkpoint tiếp tục hiện hữu, bác bỏ tình trạng chỉ còn 1-2 trade/năm trước fix. PnL các ledger vẫn âm nên đây chỉ là đóng bug reject/Make Decision, không tuyên bố chiến thuật đã có lãi; chuyển Verify để Claude review độc lập. Chi tiết research → `raw/researcher/round85-risk-fraction-still-rejected-plus-live-action-full-outage.md`.

  - **Claude verify độc lập (round `/loop` researcher tiếp theo, ~22:10 UTC+7):** SSH trực tiếp `docker inspect` cả 4 container Live Action — image `sha256:a417976330ee62d6de41d6328c7a6367fea525cd93bd9ac6bff02a7a59cd9a84` khớp đúng entry Codex, `RestartCount=0` cả 4, uptime 57 phút liên tục không flap. Đọc lại checkpoint Redis 4 route độc lập (không dựa vào số Codex báo): trade count `risk-2pct` = `fixed-pct` = `compounding-10pct` chính xác trên cả 4 route (BTC Binance 502=502=502, BTC Exness 516=516=516, XAU Binance 7=7=7, XAU Exness 392=392=392), `historical_replay_contract_version=27` đúng như kỳ vọng. Chuyển Done.

- **[trading][evidence][2026-08-22] `finance-research` đã đồng bộ Portfolio risk policy với live runtime:** commit `25329a606b4ed2764a15532efa0eefb820e22ca5` gom `PortfolioRiskPolicy::widened_for_simulation` vào `finance-core`, dùng chung cho runtime và cả hai measurement path, bổ sung CLI parity `risk_fraction` cùng regression; Docker `--cpus=3` targeted/workspace/release build và Redis integration xanh. Build and Deploy `32569004620` cùng Production Live Action Verification `32569483187` đều xanh đúng immutable SHA; chuyển Verify lúc 18:13 UTC+7, bất thường Round 85 dynamic cap được giữ thành task riêng chứ không coi là đã sửa bởi commit này.

  - **Claude verify độc lập (round `/loop` researcher tiếp theo, ~22:10 UTC+7):** SSH trực tiếp `docker inspect` cả 4 container Live Action — image `sha256:a417976330ee62d6de41d6328c7a6367fea525cd93bd9ac6bff02a7a59cd9a84` khớp đúng entry Codex, `RestartCount=0` cả 4, uptime 57 phút liên tục không flap. Đọc lại checkpoint Redis 4 route độc lập (không dựa vào số Codex báo): trade count `risk-2pct` = `fixed-pct` = `compounding-10pct` chính xác trên cả 4 route (BTC Binance 502=502=502, BTC Exness 516=516=516, XAU Binance 7=7=7, XAU Exness 392=392=392), `historical_replay_contract_version=27` đúng như kỳ vọng. Chuyển Done.


Closed/shipped work, newest first. Full original text for every item → `raw/archive/handoff_agent_pre-cleanup_2026-08-22.md`, archive line ranges noted. Most entries below already had an independent Claude verify pass (SHA-ancestor check, `gh run view`, direct production curl/SSH) before being moved here in the original log — that verification detail is preserved in the archive, not repeated here. All entries below are `evidence` priority; per-item dates were not recorded in the original log (only archive line ranges), so `Created at` is `n/a` unless noted.

- **[trading][evidence][n/a] Both production Kline alerts resolved** (Exness XAU stale recovery + Binance BTC/XAU retry-once-per-boundary fix), corroborated by 70+ rounds of monitoring with zero recurrence. Commit `c8e30b92`. (archive L2198-2218)
- **[trading][evidence][n/a] VWAP/ORB research-only backtest — no candidate promoted, PF<1 or inconsistent across splits, corroborated by rounds 18/34.** Commit `e7fb55b`. (archive L2220-2230)
- **[trading][evidence][n/a] BTC Portfolio `initial_flat`/weight-dilution bug fixed and confirmed via many rounds of direct Redis reads (rounds 64-68).** Commit `cf35652`. (archive L2232-2242)
- **[trading][evidence][n/a] `finance-research` now replays the real synchronized Portfolio decision path** — this is the ground-truth tool every subsequent `/loop` round's gate numbers depend on; cross-validated 14+ rounds with zero inconsistency. Commit `56a7f82`. (archive L2244-2261)
- **[trading][evidence][n/a] Binance XAU checkpoint epoch migration fixed** (was comparing unlike legacy vs. new decision counters); real-world effect check found Binance XAU trade_count still ~100-140x below its 3 peers, tracked separately as the `pending_history_backfill` lead. Commit `bfccc9a`. (archive L2263-2279)
- **[trading][evidence][n/a] Honest daily gate + BTC 4h/1d fixed-weight ensemble — economic gate failed as expected; Claude's own earlier manual-backtest recommendation was invalidated as a methodology error (data snooping) and retracted.** Commits `339458f`/`b6f664c`. (archive L2282-2320)
- **[observability][evidence][n/a] Kafka credential rotation complete, independently verified via CI/container health (not by re-reading the credential).** (archive L2322-2345)
- **[observability][evidence][n/a] Grafana admin credential rotated after operator-output exposure.** (archive L2347-2360)
- **[observability][evidence][n/a] Code commit-first / infra live-first / Coolify-owned delivery model formalized in the `repository-delivery` skill.** Commit `aad8ca4`. (archive L2362-2378)
- **[trading][evidence][n/a] `risk-2pct` XAU/exness no longer 100%-rejected** — root cause was notional capped at equity×leverage happening at the sizing step, not the risk-gate step. Commit `bdeaa068`. (archive L2380-2407)
- **[observability][evidence][n/a] Dead strategy cleanup + standard Python ABI + portable CI, build time cut from 20-25min.** Commits `3895176`/`02f3d59`/`813e774`/`1eded4f`. (archive L2409-2428)
- **[trading][evidence][n/a] Exness final revisions + XAU session-aware warmup fixed** (checkpoint went from 2+ hours frozen to normal 5m cadence). Commits `83180b9`/`9f27b4a`. (archive L2430-2447)
- **[observability][evidence][n/a] Canonical gRPC + OpenTelemetry deployed, live candle fetch verified end-to-end (MT5).** Commit `cbc36f6`. (archive L2449-2460)
- **[trading][evidence][n/a] Fixed floating-point risk boundary `4.000000000000001` false-reject** (epsilon tolerance, both directions tested) — explicitly does NOT fix the separate BTC Portfolio decision-burst issue (see Processing history in archive). Commit `044e2b9`. (archive L2462-2482)
- **[observability][evidence][n/a] `NOTION_API_KEY` added to `docker/env/production.env` as a fallback value, with explicit trade-off disclosure to the user before committing.** Commit `efdcaf7`. (archive L2484-2505)
- **[observability][evidence][n/a] Fixed 2 gaps blocking Notion→Telegram wiring even with a valid token** (missing Compose env declaration, missing Coolify secret sync path). Commit `7508977`. (archive L2506-2551)
- **[observability][evidence][n/a] Fixed Notion job "unknown scheduled job" — a 3rd, separate handler-registration list was missing the new job.** Commit `9acf177`, regression added. (archive L2553-2599)
- **[trading][evidence][n/a] Full kline-continuity audit (user-requested, read-only): no unexplained missing data found, monitoring confirmed healthy across 5 independent verification layers.** (archive L2601-2638)
- **[trading][evidence][n/a] Weekend-closure false "Missing N bars" on 4h/12h/1d intervals fixed** (day-of-week check instead of hour-of-day, which broke under bucket quantization). Commit `36cfc3f`. (archive L2640-2674)
- **[trading][evidence][n/a] CFD daily-rollover false "Missing N bars" (21:00-22:00 UTC break) fixed**, plus unrelated "Latest trade" chart button added same session. Commits `cba53aa`/`3e70d14`. (archive L2676-2716)
- **[observability][evidence][n/a] Docker resource limits enforced across all 32 services** — root-caused and fixed a real production Grafana/Postgres outage found mid-audit (`pids_limit` conflict between top-level and `deploy.resources.limits`, pre-existing, not caused by this work). Commits `7f96d91`/`7cb23d5`/`9ea0221`/`f9d6019`. (archive L2718-2818)
- **[observability][evidence][n/a] Notion English review → Telegram job shipped end-to-end** (4h cadence, cursor-tracked, deliberately scoped out of the shared health-check gate so a missing user-set secret doesn't spam unrelated alerts). Commit `4448118`. (archive L2820-2861)
- **[observability][evidence][n/a] Canonical ELK data stream migrated to `app-logs`** — a real incident occurred mid-migration (triplicate reindex inflated doc count, disk hit 90%); post-incident audit confirmed the legacy source is unrecoverably gone (~140,973 docs / 0.74% permanently lost, fully disclosed) and no orphan/duplicate data remains. (archive L2863-2930)
- **[observability][evidence][n/a] Production trading verifier now retries transient 502/503/504 on login instead of failing immediately.** Commit `c30a9f0`. (archive L2932-2955)
- **[observability][evidence][n/a] ELK/ECS log field standardization across all 4 repos + Kibana default data-view fix.** Commits `a1e7b8e`/`25f482f`/`c603102`/`422d968`/`bf1cf1e`/`202fd75`/`d9b48dc`. (archive L2957-2995)
- **[trading][evidence][n/a] Full kline-continuity + production hardening chain closed** (3rd independent review pass) — root cause was a single giant transaction locking Timescale chunks across all routes; fixed to per-route transactions. (archive L2997-3039)
- **[observability][evidence][n/a] Backlog-during-idle skill requirement + file-backed watchers verified live.** Commit `a3c78b6`. (archive L3041-3060)
- **[trading][evidence][n/a] Chart kline gap investigation closed** — confirmed real CFD weekend market closure, not a bug; correctly did not synthesize fake candles. (archive L3062-3075)
- **[observability][evidence][n/a] Removed legacy `/data*` route aliases + fixed Research Readiness CSS layout** (4-card responsive layout replacing a fake CSS-grid 2-column stretch). Commits `afd3c66`/`eda0ae1`. (archive L3077-3103)
- **[observability][evidence][n/a] Monitoring coverage extended to MT5 + fixed a Grafana dashboard-verification race condition** (verify ran before vmagent's first scrape cycle, not a real rollout failure). Commits `ab96363`/`2ee4778`. (archive L3104-3128)
- **[observability][evidence][n/a] Frontend restructured to `app/features/shared` boundaries with an enforced architecture contract, zero behavior change.** Commits `13b0ee2`/`38a4a8a`/`9e3fef3`. (archive L3129-3180)
- **[observability][evidence][2026-08-14] Incident chain 2026-08-14 (07f63ad→f81c419, ~4h) fully closed** — cascading: (1) `python3` call broken after Python removal from image → replaced with a Go binary; (2) Live Action history-not-ready after downtime from a dropped bootstrap block → restored + evidence-clock fixes for no-lookahead false alerts; (3) Grafana false alerts from `noDataState` misconfiguration + stale inode/Kafka-lag math → fixed. All 10 cited commits confirmed on `origin/main`, all CI runs green, production endpoints 200 at final review. (archive L3184-3264)

### Batch đã deploy (2026-08-14, SHA `76911d2`)

- Removed all references to a retired production address from skills/docs/config. Commit `e2a7108`.
- Portfolio rule now preserved correctly across Realtime/Backtest mode switches.
- Native automation migrated from Python to Go (`internal/automation`), Python fully removed from the runtime image.
- `scripts/` and `.github/` cleaned up of orphaned Python/command-handler leftovers.
(archive L3265-3289)

### Verify trước đó (older, already re-confirmed)

- Grafana `No data` panels fixed via live SSH apply + committed source. Commit `8fc8b71`.
- Multica's `ops/` native systemd fully removed. Commit `cab3213`/`76911d2`.
- Post-deploy production verification checklist rule added (docs-only).
- Canonical `/metrics` + observability bootstrap (4-repo `setup-log.sh`/`setup-agent.sh` contract) live and verified.
- ECS/Filebeat/Kibana + disk recovery maintenance runs all green.
- Windmill fully migrated off and removed; native cron owns all 13 schedules.
- TimescaleDB retention raised to 5 years, confirmed in production policy.
- Finance MW + Live Action 5-year backfill/replay shipped and verified.
- Chart PR #197, kline continuity PR #199, `repository-delivery` skill PR #198 all merged and verified live.
- `raw/archive/closed-2026-08/system-review-strategy-optimization.md` P1-P4 reconciled as shipped; P5 (news/GDELT) explicitly out of scope.
- REST `/trade-state` removed (finance-mw PR #200); scope-blind history/unary state fail-closed (finance-live-action PR #94).
(archive L3291-3344)
