[project]
name = "phase-agent-orchestrator"
version = "0.1.0"
description = "Python implementation of the Finance phase-agent orchestration state CLIs"
readme = "README.md"
authors = [
    { name = "thanhnd13", email = "thanhnd13@vng.com.vn" }
]
requires-python = ">=3.14"
dependencies = [
  "claude-agent-sdk==0.2.151",
  "openai-codex==0.147.0",
    "pyyaml>=6.0.3",
]

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"

[project.scripts]
classify-claude-result = "phase_agent_orchestrator.cli.classify_claude_result:cli"
classify-codex-result = "phase_agent_orchestrator.cli.classify_codex_result:cli"
configure-phase-agents = "phase_agent_orchestrator.cli.configure_phase_agents:cli"
detect-codex-availability = "phase_agent_orchestrator.cli.detect_codex_availability:cli"
detect-provider-availability = "phase_agent_orchestrator.cli.detect_provider_availability:cli"
ops-runtime = "phase_agent_orchestrator.cli.ops_runtime:cli"
coordinator = "phase_agent_orchestrator.cli.coordinator:cli"
phase-agent-state = "phase_agent_orchestrator.cli.phase_agent_state:cli"
quant-research-state = "phase_agent_orchestrator.cli.quant_research_state:cli"
run-claude-phase = "phase_agent_orchestrator.cli.run_claude_phase:cli"
run-codex-phase = "phase_agent_orchestrator.cli.run_codex_phase:cli"
run-phase-agent = "phase_agent_orchestrator.cli.run_phase_agent:cli"
run-phase-agent-command = "phase_agent_orchestrator.cli.run_phase_agent_command:cli"
sync-agent-links = "phase_agent_orchestrator.cli.sync_agent_links:cli"
wait-for-phase-attempt = "phase_agent_orchestrator.cli.wait_for_phase_attempt:cli"
watch-phase-attempt-log = "phase_agent_orchestrator.cli.watch_phase_attempt_log:cli"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
