## 1. Verification-mode contract

- [x] 1.1 Update `.claude/commands/ops/run.md` so FINAL_VERIFY, release, and completion branch on persisted `verification_mode`; verify Codex remains independent and fallback uses enhanced self-review with no independent claim.
- [x] 1.2 Extend the backend-routing behavioral test for verification wording, invalid unconditional gates, and fallback state preservation through FINAL_VERIFY; verify it launches no real agent, release, loop, or deployment.

## 2. Terminal operation archive

- [x] 2.1 Move `finance-mw-dev-docs-smoke` from `.ops/changes` to the dated `.ops/archive` path while preserving FAILED timeout, cleaned-lock, and no-deployment evidence; verify the active namespace has no terminal smoke record.

## 3. Verification and delivery

- [x] 3.1 Run all required bounded shell suites, syntax, JSON, managed-link, strict OpenSpec, and diff checks; verify accepted backend/FIX/quant/generic-worker behavior remains unchanged.
- [x] 3.2 Complete skill upsert review, archive OpenSpec/runtime state, commit scoped files, push fast-forward to `main`, verify exact remote SHA, and track Agent Contracts success.
