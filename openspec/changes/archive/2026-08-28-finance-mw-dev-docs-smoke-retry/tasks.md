## 1. Documentation implementation

- [x] 1.1 Inspect `finance-mw` instructions, Makefile, Compose/configuration, and existing docs; identify the documentation file and verify every command, dependency, and health path used by the guide.
- [x] 1.2 Add the concise local-development and health-verification section to the identified existing `finance-mw` developer documentation file, and verify no runtime or unrelated files changed.

## 2. Smoke-test verification

- [x] 2.1 Independently inspect the final documentation diff, verify its commands against repository evidence, run `git diff --check`, and confirm the change is dev-only with production deployment intentionally skipped.
