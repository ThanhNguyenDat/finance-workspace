# route-quant-promotions-through-ops

- Claude: workflow initialized; OpenSpec plan and affected workspace contracts prepared.
- Codex: implementing bounded trace-origin, promotion routing, source-of-truth migration,
  and offline contract coverage. No production deployment is in scope.
- Verification: local checks and Agent Contracts CI are required before delivery;
  independent Claude verification remains a separate gate.
